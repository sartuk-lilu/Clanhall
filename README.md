# План Разработки Прототипа — Технический документ

## Стек и архитектурные решения

**Engine:** Unreal Engine 5  
**Плагин:** Gameplay Ability System (GAS)  
**Паттерн данных:** DataAsset + Fragments (композиция через данные)  
**Язык:** C++ для ядра, Blueprint для быстрого прототипирования навыков

---
## Design-docs 

| ![design-doc](https://github.com/user-attachments/assets/fae4721f-fc54-47a4-b6cb-4d64d4d72d8e) |
| ---------------------------------------------------------------------------------------------- |

---
## Архитектура DataAsset + Fragments

Центральный паттерн проекта. Каждый навык, заклинание или оружие — это `UPrimaryDataAsset` с заголовком и массивом фрагментов.

### Почему Fragments

Без фрагментов: один класс на 50 полей, большинство — null в зависимости от типа навыка.  
С фрагментами: заголовок содержит только то, что есть у каждой абилки. Фрагмент — только то, что нужно конкретной.

### Структура DataAsset (заголовок)

```cpp
UCLASS()
class UAbilityData : public UPrimaryDataAsset
{
    // Заголовок — есть у каждой абилки без исключения
    FText DisplayName;
    UTexture2D* Icon;
    float Cooldown;
    int32 ChargeCost;             // 0 / 2 / 4 / 6
    UAnimMontage* CastMontage;    // nullptr законен — механика работает без монтажа
    float BalanceShift;           // МОДУЛЬ сдвига шкалы; знак — из Weapon.Type.*

    // Фрагменты — только то что нужно конкретной абилке
    UPROPERTY(EditAnywhere, Instanced)
    TArray<TObjectPtr<UAbilityFragment>> Fragments;

    template<typename T>
    T* FindFragment() const;  // запрос фрагмента по типу → nullptr если нет
};
```

### Ключевые спецификаторы базового класса фрагмента

```cpp
UCLASS(Abstract, DefaultToInstanced, EditInlineNew)
class UAbilityFragment : public UObject {};
// DefaultToInstanced — каждый экземпляр в массиве уникален
// EditInlineNew    — редактор разворачивает содержимое прямо внутри DataAsset
```
### Граница «заголовок или фрагмент»

Фрагмент оправдан тогда, когда его **отсутствие** несёт смысл, невыразимый дефолтным значением поля. Нет такого смысла — поле идёт в заголовок: фрагмент дал бы только лишний клик в редакторе и молчаливый баг «забыл добавить».

Поэтому `CastMontage` (отсутствие ≡ `nullptr`) и `BalanceShift` (отсутствие ≡ `0`) живут в заголовке, а `UDamageFragment` — фрагмент: его отсутствие означает «утилитарный навык, попадание подтверждается фактом найденной цели», чего `BaseDamage = 0` не выражает.

**Знак сдвига баланса в данных не хранится.** По `combat_system.md §2` он однозначно следует из типа оружия (STR → вправо, DEX → влево) и резолвится из `Weapon.Type.*` на ASC одной функцией `UGA_ClanhallAbilityBase::GetBalanceSign` — общей для WASD-ударов и активок. В ассете лежит только модуль (`ClampMin = 0`).

### Фрагменты проекта

| Фрагмент | Поля | Когда добавлять |
|---|---|---|
| `UVFXFragment` | CastEffect, ImpactEffect | У всех с визуалом |
| `USFXFragment` | CastSound, ImpactSound | У всех со звуком |
| `UDamageFragment` | BaseDamage | У наносящих урон |
| `UMarkApplyFragment` | MarkTag | У накладывающих метку |
| `UMarkTriggerFragment` | TArray\<FMarkSynergy\> | У потребляющих метки |
### FMarkSynergy — структура внутри UMarkTriggerFragment

```cpp
USTRUCT()
struct FMarkSynergy
{
    FGameplayTag RequiredMark;          // метка-условие на цели
    TSubclassOf<UGameplayEffect> EffectOnTarget;  // дебафф на врага
    TSubclassOf<UGameplayEffect> EffectOnSelf;    // бафф на себя
    // Только одно из двух заполнено — никогда оба
    int32 ChargeGain = 2;               // заряды за срабатывание
};
```

**Две тонкости заполнения.** `RequiredMark` матчится через `MatchesTag`, а не равенством: конкретный тег (`Mark.BrokenGuard`) матчит только себя, корневой `Mark` — любую метку (так выражается «активирует независимо от типа метки» у Retribution). Следствие: выигрывает ПЕРВОЕ совпадение в массиве, поэтому конкретные записи ставятся ВЫШЕ широких. `ChargeGain` — число в данных, а не в коде (`mark_system.md §2/§4`).
### Как GameplayAbility читает фрагменты

Абилка данных не содержит — только запрашивает через `FindFragment<T>()`:

```cpp
// Резолв одной задетой цели — вызывается из окна контакта либо из мгновенного фолбэка
void UGA_PhysicalSkill::ResolveHitOn(AActor* Target)
{
    // Урон — через фрагмент; его отсутствие = утилитарный навык
    if (auto* Dmg = AbilityData->FindFragment<UDamageFragment>())
        bConfirmedHit = ResolveStandardDamage(SourceASC, TargetASC, Dmg->BaseDamage);

    if (!bConfirmedHit) return;

    // Синергия по метке цели и наложение своей — оба через фрагменты
    ResolveMarkLogic(AbilityData, SourceASC, TargetASC, TargetMarkComponent);

    // Состояние на себя — один раз за применение, сколько бы целей ни задело
    if (!bBalanceApplied && !FMath::IsNearlyZero(AbilityData->BalanceShift))
        ShiftBalance(GetBalanceSign(SourceASC) * AbilityData->BalanceShift);
}
```

Логика абилки не меняется при изменении данных — только DataAsset.

`CastMontage` в этом списке больше не «косметика последней строкой»: он запускается до резолва и сам решает, будет ли резолв контактным или мгновенным (Раздел 4). Но правило «механика раньше визуала» уцелело: `nullptr` — законное состояние, при нём навык работает целиком через мгновенный фолбэк.
### Порядок добавления фрагментов (не всё сразу)

1. Заголовок + `UDamageFragment` — навык наносит урон и двигает шкалу (`BalanceShift` — тоже заголовок)
2. `UMarkApplyFragment` + `UMarkTriggerFragment` — система меток работает
3. `CastMontage`, `UVFXFragment`, `USFXFragment` — визуал и звук

**Правило:** сначала механика работает, потом она красиво выглядит. `CastMontage` в заголовке этому не мешает: `nullptr` — законное состояние до нарезки монтажей.


---
### Таксономия GameplayTags

Теги закладываются **один раз и полностью**. Дописывать новые можно свободно. **Переименовывать — нельзя:** ассеты (`UAbilityData`, `GameplayEffect`, Blueprint, Data Table) хранят тег строкой `FName`, и после переименования в C++ ссылка в ассете указывает в пустоту — GAS молча перестаёт матчить, без ошибки сборки. Ломается ссылка, а не тег.
**Удалять тег, на который никто не ссылается — можно**, сначала проверив Reference Viewer (Project Settings → GameplayTags → поиск ссылок). Мёртвые теги «заделом» не держим: завести заново — одна строка, а лежащий без дела тег с устаревшим комментарием активно врёт.

```
Ability.Skill.Knight.*
Ability.Skill.Warrior.*
Ability.Skill.Assassin.*
Ability.Skill.Lancer.*


Ability.Class.Knight
Ability.Class.Warrior
Ability.Class.Assassin
Ability.Class.Lancer

Mark.*

State.Casting
State.CastingAntimagic
State.Parrying
State.CounterWindow
State.InStance
State.Stunned
State.Knockdown
State.ComboRecovery
State.SkillCommitted

Unit.Role.Mob
Unit.Role.Boss.Humanoid
Unit.Role.Boss.Monster

Weapon.Type.STR
Weapon.Type.DEX

Balance.Overload.STR
Balance.Overload.DEX

Magic.School.Elemental.Rank.*
Magic.School.Aether
Magic.School.Materia
Magic.School.Stars

Cooldown.Slot.Q
Cooldown.Slot.E
Cooldown.Slot.R
Cooldown.Slot.F
Cooldown.Slot.Z
Cooldown.Slot.X
Cooldown.Slot.C
Cooldown.Slot.V

Attack.Direction.W
Attack.Direction.A
Attack.Direction.S
Attack.Direction.D

Event.ApplyMark
Event.DirectionalAttack
Event.Hitbox.Hit
Event.Hitbox.Closed

Damage.Type.Slash
Damage.Type.Pierce
Damage.Type.Blunt

SetByCaller.Magnitude
Perk

```

---
## Прототип магической системы и симуляция боевой системы

| ![CombatSystemSimulation](https://github.com/user-attachments/assets/26b030b2-c058-4553-8d0d-1cebcd7c49d8) |
| ---------------------------------------------------------------------------------------------------------- |
| ![spells_prototype](https://github.com/user-attachments/assets/dcc6b793-cff8-4e09-945a-52b17984e5e3)       |


---

## Реализация и статус разработки

| #   | Система                    | Статус      |
| --- | -------------------------- | ----------- |
| 1   | Фундамент GAS и атрибуты   | ✅ Готово    |
| 2   | Боевая стойка и WASD-удары | ✅ Готово    |
| 3   | Система меток              | ✅ Готово    |
| 4   | DataAsset, Fragments       | ✅ Готово    |
| 5   | Парирование                | ✅ Готово    |
| 6   | Контрнавык                 | ✅ Готово    |
| 7   | Animation Setup            | ✅ Готово    |
| 8   | Противники                 | In Progress |
| 9   | Магическая система         |             |
| 10  | Смена оружия               |             |
| 11  | Искусственный интеллект    |             |
| 12  | Колесо умений              |             |

---

### Раздел 1 — Фундамент GAS и атрибуты
**Статус:** ✅ Готово.

**Итог:** GAS подключён, `AbilitySystemComponent` живёт на Character и Enemy, все пять ресурсов существуют как атрибуты и клампятся в каноничных границах, полная таксономия тегов заложена одним файлом.

---

| Реализация                             | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`UClanhallAttributeSet`**            | (`AbilitySystem/ClanhallAttributeSet.h/.cpp`) — одиннадцать `FGameplayAttributeData`: AP/MaxAP, HP/MaxHP, MP/MaxMP, Charges/MaxCharges, Balance и Stagger/MaxStagger (последняя пара — Раздел 8, Блок D, усталость от парирования). У Balance намеренно нет MaxBalance — диапазон жёстко зашит −100..+100 прямо в клампе. Комбинированный макрос `ATTRIBUTE_ACCESSORS` (getter + value getter/setter/initter) объявлен в проекте сам, т.к. движок даёт только четыре отдельных «кирпича» (паттерн Lyra / GASDocumentation).                                                            |
| **Кламп в двух точках, а не в одной.** | `PreAttributeChange` ловит мгновенные правки из кода (напр. `SetHP`). Но модификаторы `GameplayEffect` идут через Aggregator и могут на шаг обогнать пересчёт лимита, поэтому `PostGameplayEffectExecute` повторно клампит итоговое значение. Ресурсы → `[0, Max]`, Balance → `[−100, 100]`.                                                                                                                                                                                                    |
| **Репликация заложена сразу**          | (задел под мультиплеер, хотя прототип однопользовательский): все одиннадцать атрибутов через `DOREPLIFETIME_CONDITION_NOTIFY(COND_None, REPNOTIFY_Always)` + свой `OnRep_*` на каждом (`GAMEPLAYATTRIBUTE_REPNOTIFY`).                                                                                                                                                                                                                                                                               |
| **Таксономия GameplayTags**            | `AbilitySystem/ClanhallGameplayTags.h`, нативные теги через `UE_DECLARE_GAMEPLAY_TAG_EXTERN` (не DataTable). Заложены полностью и сразу: `Ability.Class.*`, `Ability.Skill.*` (листья Knight — под Разделы 4/6), `State.*`, `Weapon.Type.*`, `Balance.Overload.*`, `Parry.Incoming.*`, `Cooldown.Slot.*`, `SetByCaller.Magnitude`, `Event.*`, `Damage.Type.*`, `Perk`, `Magic.School.*` (только корни — ранги отложены до Раздела 9), `Unit.Role.*`, `Mark.*` (полные 33 метки + `Compressed`). |
| **Правило (GameplayTags):**            | дописывать теги можно, переименовывать — нет (ассеты хранят тег строкой, ссылка порвётся молча). Удалять неиспользуемый тег можно — после проверки Reference Viewer.                                                                                                                                                                                                                                                                                                                            |

---

**Проверка:** атрибуты создаются на обоих ASC, значения держатся в границах при любом источнике изменения, HUD-минимум читает AP/HP/MP.


---

### Раздел 2 — Боевая стойка и WASD-удары
**Статус:** ✅ Готово.

**Итог:** LMB-hold переводит в боевую стойку, в стойке W/A/S/D — четыре направленных удара с AP-обменом, регеном MP и сдвигом Balance; вне стойки те же клавиши просто двигают персонажа.

---

| Реализация                              | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Стойка**                              | `UGA_CombatStance` (`Abilities/GA_CombatStance.h/.cpp`), `InstancedPerActor`. Пока активна, вешает `State.InStance` (`ActivationOwnedTags`); повторный вход заблокирован тем же тегом в `ActivationBlockedTags`. Привязка LMB-hold/release — на стороне персонажа/контроллера.                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Вход закрыт на время Recovery**       | `ActivationBlockedTags` дополнительно содержит `State.ComboRecovery`: пока доигрывает Recovery-анимация, в стойку не войти (Раздел 7). ВЫХОД свободен всегда — он идёт через `CancelAbilityHandle` и `ActivationBlockedTags` не касается.                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Ретрай ввода**                        | `StanceAction` привязан не только к `Started`, но и к `Triggered`. Без этого нажатие во время лока проглатывалось бы: `Started` израсходован, и после спадания тега персонаж так и не вошёл бы в стойку при зажатой ЛКМ. `Triggered` повторяет попытку каждый кадр удержания; при уже активной стойке это дешёвый отказ по `State.InStance`.                                                                                                                                                                                                                                                                                                                                          |
| **Стойка гасит движение мгновенно**     | `UGA_CombatStance::ActivateAbility` зовёт `StopMovementImmediately()` на аватаре. `DoMove` и так не принимает WASD в стойке, но остаточная инерция от бега гаслась бы за пару десятых; снап в 0 роняет Speed в тот же кадр, чтобы переход Locomotion→CombatStance блендился из idle-позы, а не из бега. Фолбэк, если снап читается резко: высокий `BrakingDecelerationWalking` на время стойки (скид-ин) вместо мгновенного стопа.                                                                                                                                                                                                                                                    |
| **В стойке нельзя прыгать**             | `AClanhallCharacter::CanJumpInternal_Implementation()` возвращает `false` при `State.InStance`. `JumpAction` привязан к `ACharacter::Jump` напрямую, но фактический прыжок проходит через `CanJump()`/`CanJumpInternal()` в movement-тике — это единственная точка отсечения, входные биндинги трогать не нужно. Когда Раздел 9 введёт umbrella-тег `State.Stance`, проверку перевести на него вместе с `DoMove` — прыжок блокируется во всех трёх стойках.                                                                                                                                                                                                                           |
| **Гейт на стойку для всех физдействий** | Общий предок `UGA_ClanhallAbilityBase` ставит `ActivationRequiredTags = State.InStance`. Поэтому вне стойки WASD-абилки просто не активируются (`TryActivateAbility` отказывает), и клавиши работают как движение — без отдельной ветки кода.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **WASD-удары**                          | база `UGA_DirectionalAttackBase` (`InstancedPerExecution`) + четыре тонких наследника (`GA_DirectionalAttacks.h`), каждый переопределяет только `GetDirection()` → `Overhead(W)/RightSlash(D)/LeftSlash(A)/LowSweep(S)`. Enum `EClanhallAttackDirection` вынесён в `ClanhallCombatTypes.h`, чтобы не плодить взаимозависимости заголовков (его же читают Parry- и Hitbox-компоненты).                                                                                                                                                                                                                                                                                                 |
| **Поиск цели**                          | (геометрия мгновенного фолбэка и запрос цели для контрнавыка, Раздел 7)** `FindMeleeTarget` в базовом классе: `SphereOverlapActors` сферой перед персонажем (`TraceRange 200`, `TraceRadius 75`), берётся первый актор с `IAbilitySystemInterface`.                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **AP-обмен**                            | `ResolveStandardDamage` (в базовом классе): у цели снимается `min(урон, AP цели)`, 50 % снятого возвращается атакующему в свой AP, переполнение (урон больше остатка AP) идёт прямо в HP. Возврат `true` = «confirmed hit» — на нём завязаны КД и синергии. Всё через generic-эффекты `GE_Modify*` с SetByCaller-магнитудой.                                                                                                                                                                                                                                                                                                                                                          |
| **MP и Balance на удар**                | (`GA_DirectionalAttackBase::ResolveHitOn`): STR-оружие (`Weapon.Type.STR` на ASC) → MP +10, Balance +5..+15; DEX → MP +5, Balance −5..−15 (сдвиг рандомный в диапазоне). Само число урона приходит из комбо-компонента через `TriggerEventData->EventMagnitude` (Раздел 7) — но STR/DEX-логика MP/Balance осталась здесь. Применяются вместе с уроном — в момент контакта зоны либо на активации в фолбэк-режиме.                                                                                                                                                                                                                                                                     |
| **Мультицель у WASD**                   | то же разделение «ресурс / состояние», что у активок** (`mark_system.md §2 Правило 5`). Взмах задевает всех, кого прошла зона (`AlreadyHit` не даёт ударить одного дважды). **Ресурс** — урон, возврат AP и MP — на каждую задетую цель. **Состояние** — сдвиг Balance — один раз за взмах, гейт `bBalanceApplied`. Гейт стоит внутри ветки подтверждённого попадания: взмах, который коснулся цели, но урона не нанёс, шкалу не двигает. Сброс вручную не нужен — `InstancedPerExecution` даёт свежий инстанс на каждую активацию. Фолбэк-путь пока одноцелевой (`FindMeleeTarget` возвращает одну ближайшую цель) — расхождение исчезнет, когда все монтажи будут размечены зонами. |
| **Пассивный дрейф Balance к нулю**      | `GE_BalanceDrift` (Infinite, `Period 1.0` сек, без execute-on-application) + `ExecCalc_BalanceDrift`: шаг 2 ед/тик к центру, с клампом `Sign*Drift` в пределах `±\|текущее\|`, чтобы не проскочить ноль на другую сторону.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

---


```
**Пройденные этапы:**

- **Сдвиг Balance перестал умножаться на число целей.** 
  После перехода на контактный резолв (Раздел 7) `ResolveHitOn` стал вызываться на каждую задетую цель, а MP и Balance остались внутри — взмах по троим давал три независимых рандомных сдвига, до 45 единиц при пороге перегруза ±60. Два пути урона в игре при этом жили по разным правилам: `GA_PhysicalSkill` разделение уже соблюдал (`bBalanceApplied`, `bSelfSynergySpent`), WASD — нет. Исправлено тем же паттерном, без изобретения второго.
  
- **Мультицель у WASD оставлена осознанно.** 
  Альтернатива («взмах бьёт только ближайшего») требовала бы дополнительного кода против собственной геометрической модели — урон резолвит зона, а не выбор цели. Какие удары бьют по площади, следует из формы зоны бесплатно: широкие A/D/S проходят через двоих, узкий вертикальный W — почти никогда.
```

---

**Проверка:** в стойке удар по болванчику — AP уходит туда-обратно (−цель/+50 % себе), MP растёт, Balance ползёт от направления оружия и сам возвращается к нулю в простое.


---

### Раздел 3 — Система меток
**Статус:** ✅ Готово.

**Итог:** у каждого бойца свой независимый трек метки — таймер-тег на компоненте, максимум одна метка, с различением «своя/чужая» по источнику.

---

| Реализация                                          | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`UClanhallMarkComponent`**                        | (`AbilitySystem/ClanhallMarkComponent.h/.cpp`) — по одному экземпляру на игрока и на каждого врага (два независимых трека, `mark_system.md §3, §5`).                                                                                                                                                                                                                                                                                                                  |
| **Метка = таймер-тег на ASC**                       | через `ClanhallGameplayEffects::ApplyTimedTag`, длительность 5 сек (`MarkDurationSeconds`). Максимум одна: `ApplyMark` сначала зовёт `ClearMark` (`RemoveActiveGameplayEffect` старого хендла), стека нет.                                                                                                                                                                                                                                                            |
| **Источник метки**                                  | `CurrentMarkSourceASC` (`TWeakObjectPtr`), `IsOwnMark(QueryASC)`. Потребителя в геймплее сейчас нет — синергия смотрит только на тег цели и в источнике не нуждается. Метод сохранён под HUD («кто повесил метку») и синергии врага в Разделе 8. Метку, наложенную врагом, игрок снять не может никак — только переждать 5 сек.                                                                                                                                       |
| **Кэш ≠ истина**                                    | `CachedMarkTag` — только подсказка «какой тег проверять»; истина всегда в теге на ASC. `GetCurrentMark()` каждый раз перепроверяет `HasMatchingGameplayTag`, потому что по истечении 5 сек GE снимает тег сам, а кэш мог не узнать об этом мгновенно.                                                                                                                                                                                                                 |
| **Проверка синергии, активация и генерация заряда** | живут в `GA_PhysicalSkill` (Раздел 4), метод `ResolveMarkLogic`: метка на цели сгорает → эффект срабатывает (бафф на себя ИЛИ дебафф на цель, никогда оба) → навык кладёт свою новую метку.                                                                                                                                                                                                                                                                           |
| **Заряд синергии — число в данных**                 | `FMarkSynergy::ChargeGain` (дефолт 2). Зашитых чисел меток и синергий в коде не осталось.                                                                                                                                                                                                                                                                                                                                                                             |
| **Мультицель — разделение «ресурс / состояние»**    | не «на цель / на себя» (`mark_system.md §2 Правило 5`). Урон, наложение метки, `EffectOnTarget` и `ChargeGain` — на каждую задетую цель; `EffectOnSelf` и сдвиг Balance — один раз за применение навыка. Заряды масштабируются числом целей намеренно: бой рассчитан на одного противника, изредка на 2–3, то есть мультицель — редкая ситуация повышенного риска, и она должна награждать. От перекоса защищает кламп `MaxCharges`, а не отдельное правило убывания. |
| **HUD**                                             | цветной индикатор типа метки + таймер 5 сек над врагом.                                                                                                                                                                                                                                                                                                                                                                                                               |

---

**Проверка:** вручную повесить метку, вторым навыком активировать синергию — эффект срабатывает, заряд добавляется, новая метка ложится.


---

### Раздел 4 — DataAsset, Fragments
**Статус:** ✅ Готово.

**Итог:** один класс-навык `UGA_PhysicalSkill` обслуживает все физические активки; всё их содержание (урон/метка/синергия/баланс/КД/стоимость) — данные в `UAbilityData`, меняются без перекомпиляции.

----

| Реализация                          | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **`UAbilityData`**                  | (`AbilitySystem/AbilityData.h`) : `UPrimaryDataAsset`. Заголовок: `DisplayName`, `Icon`, `Cooldown` (по умолч. 10), `CounterTag` (`Ability.Skill.*` — идентичность, «чем я контрю»), `CounteredBy` (`FGameplayTagContainer`, `Ability.Skill.*` — уязвимость, «чем ЭТОТ навык можно сбить»), `CounterStunDuration` (свойство СБИТОГО навыка, плейсхолдер 0.5 сек), `ChargeCost` (0), `CastMontage`, `BalanceShift` (модуль, `ClampMin = 0`), массив `Fragments` (Instanced). Шаблон `FindFragment<T>()` возвращает первый фрагмент типа T или nullptr. Слот КД (`Cooldown.Slot.*`) и требуемый класс — не поля ассета, а свойства гранта (Раздел 8, Блок A2): слот приходит ключом `UClassKitData::Skills` и живёт как динамический тег спека, класс — тем, в каком ките ассет лежит. Ассет симметричен — одно и то же описание обслуживает и игрока, и врага (Раздел 8, Блок C): враг активирует `UGA_PhysicalSkill` с тем же `SourceObject`, что игрок, `GA_EnemyActiveSkill` больше не существует.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Фрагменты**                       | База `UAbilityFragment` (`Abstract, DefaultToInstanced, EditInlineNew` — массив полиморфных подобъектов, редактируемых прямо внутри ассета). Механические (`GameplayFragments.h`): `UDamageFragment`(BaseDamage), `UMarkApplyFragment`(MarkTag), `UMarkTriggerFragment`(TArray\<FMarkSynergy\>). Презентационные (VFX/SFX) вынесены в `PresentationFragments.h`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Один класс на все активки**       | `UGA_PhysicalSkill` (`InstancedPerExecution`) гранится 4 раза (Shield Slam / Power Strike / Shield Charge / Retribution), каждый раз со своим `SourceObject` (=`UAbilityData`) через `FGameplayAbilitySpec::SourceObject`. `GetAbilityData` достаёт `SourceObject` через `Handle`, а не `GetCurrentSourceObject()` — у `InstancedPerExecution` на момент `CanActivateAbility` персистентного инстанса ещё нет (метод может вызваться на CDO).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Активка коммитится на активации** | `CanActivateAbility` проверяет тег КД и наличие Charges. Charges и тег КД (`ApplyTimedTag` на `Data->Cooldown`) списываются на активацию БЕЗУСЛОВНО, включая промах и контр: освобождения от КД для контра не существует. Заблокированный канон «КД только при confirmed hit» отменён осознанно.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Порядок в `ActivateAbility`**     | поиск цели (для фолбэка) → списание Charges → тег КД → погасить чужую WASD-серию и сыграть `CastMontage` (если задан), ДО подписки на события Hitbox → выбор режима резолва. Резолвер контрнавыка (`TryResolveCounter`) сюда больше не входит — он переехал в `ResolveHitOn` (см. ниже).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Два режима резолва урона**        | тот же паттерн, что у WASD-ударов. `UAnimNotifyState_Hitbox::MontageHasHitbox(Data->CastMontage)` решает: **контактный** (монтаж есть и на нём расставлена зона) — способность ждёт `Event.Hitbox.Hit` через `UAbilityTask_WaitGameplayEvent` и резолвит через `ResolveHitOn` каждую цель, задетую зоной; **мгновенный фолбэк** (`CastMontage == nullptr` или зоны на нём нет) — тот же `ResolveHitOn` вызывается один раз, сразу на активации, по цели из `FindMeleeTarget`. `ResolveHitOn`: урон через `UDamageFragment`/`ResolveStandardDamage` (утил-навык без урона считается попавшим по факту найденной цели) → **контр по контакту** (`TryResolveCounter`) → **метка/синергия/`ChargeGain`, гейченные `MarkResolvedTargets`** (один раз на цель за применение) → сдвиг Balance (один раз за применение) → debug. Три уровня применения, а не два: «за контакт» (урон, контр) / «за цель за применение» (метка, синергия) / «за применение» (Balance, `EffectOnSelf`) — иначе вторая фаза мультизонного удара увидела бы метку, положенную первой, и синергия с корневым `RequiredMark` (Retribution) сработала бы на собственной метке. Контру отдельный гейт не нужен: `ConsumeCounter` закрывает окно, и повторный контакт по той же цели вернёт `false`. Ветка промаха/непопадания: заряды списаны, КД повешено на активации, метка не ложится ни на кого (Раздел 3). Монтажи всех четырёх ассетов Knight назначены и играются; режим резолва у каждого зависит только от того, расставлена ли на его монтаже зона (Раздел 7, Блок D п.10). |
| **Терминатор способности**          | — конец каст-монтажа**, не `Event.Hitbox.Closed`.** `Closed` означает «сейчас нет открытых зон», а не «удар закончился»: между двумя последовательными зонами (щит закрылся — меч ещё не открылся) список пустеет, и старый терминатор оборвал бы способность в промежутке, теряя второй урон. Способность подписывается на `Montage_SetEndDelegate` после `Montage_Play(Data->CastMontage)` — делегат срабатывает и на нормальном завершении, и на прерывании. На `Event.Hitbox.Closed` активка больше не подписана вовсе. Актуальное правило — в таблице Раздела 7: первичный терминатор — конец монтажа всегда, когда монтаж стартовал, а способность ждёт позднейший из двух терминаторов — его и конец рывка (`TryFinishAbility`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **`State.SkillCommitted`**          | Вешается loose-тегом в `ActivateAbility`, когда каст-монтаж стартовал (либо монтажа нет, но запущен рывок), снимается единственной точкой в переопределённом `EndAbility`. В `ActivationOwnedTags` тега нет намеренно: на фолбэк-пути без монтажа и без рывка он повесился бы вхолостую. Блокирует вторую активку и WASD-серию (`ActivationBlockedTags` на `UGA_ClanhallAbilityBase`), а с Раздела 7 — ещё движение и прыжок. Обоснование границ — в таблице Раздела 7 («Активку нельзя прервать вручную»).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Мультизонные удары**              | Несколько `AnimNotifyState_Hitbox` на одном монтаже = несколько уронов, всегда — развилки «две формы одного контакта» не существует, `HitGroup`/attack ID не нужны. Урон гейтится только `AlreadyHit` зоны (уже было); метка/синергия/`ChargeGain`/контр — `MarkResolvedTargets` навыка, один раз на цель. Правило разметки: все Hitbox-нотифаи одного монтажа — на одной дорожке (движок разрешает пересечение на разных, это наш запрет).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

---

```
**Пройденные этапы:**

- **`CastMontage` и `BalanceShift` переехали из фрагментов в заголовок.** 
  Изначально были отдельными `UAnimationFragment` и `UBalanceFragment`. Причина переезда — критерий из «Граница «заголовок или фрагмент»»: отсутствие обоих не несло смысла сверх `nullptr`/`0`, то есть фрагмент давал только лишний клик в редакторе и молчаливый баг «забыл добавить — анимации нет, логика работает». Вместе с `UAnimationFragment` удалено поле `ImpactMontage`: реакция на удар принадлежит получателю, а не навыку атакующего, и станет отдельной системой.
  
- **Знак сдвига баланса перестал храниться в данных.**
  Раньше в ассете лежало со знаком число; теперь только модуль, а знак резолвится из `Weapon.Type.*` общим `GetBalanceSign` — одним на WASD-удары и активки. Правило из `combat_system.md §2` (STR → вправо, DEX → влево) стало жить в одной точке, а `ClampMin = 0` сделал невозможной опечатку знаком на масштабе 8 слотов × 8 оружий.
```

---

**Проверка:** нажатие Q → Shield Slam, метка ложится, синергия из Раздела 3 срабатывает; правка цифр в DataAsset меняет поведение без перекомпиляции.


---

### Раздел 5 — Парирование
**Статус:** ✅ Готово.

**Канонический дизайн** (`combat_system.md §5`): Clash Detection — weapon-trace игрока пересекается с weapon-trace врага в активном окне анимации, без UI-индикаторов. **Полная trace-версия — в Разделе 7** (диспетчер зон `UClanhallHitboxComponent`). Ниже — то, что реализовано в этом разделе как placeholder до готовых монтажей.

---

| Реализация                                           | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`UClanhallParryComponent`**                        | (`AbilitySystem/ClanhallParryComponent.h/.cpp`) на Character: флаг `bParrySuccessful`, `ResetParry()`, `TryParry(HitEnemy, PlayerDirection, HitLocation)`. Маппинг направления удара игрока → парируемый тег `Parry.Incoming.*` по правилу «обратное направление»: W парирует входящий S, S→W, D→A, A→D. Проверяет, что нужный `Parry.Incoming.*` висит на ASC игрока, ставит флаг, играет clash-звук.                                                                                                                                                                                          |
| *Точка вызова переехала*                             | В Разделе 5 `TryParry` дёргался из обработчиков ввода WASD; теперь его зовёт диспетчер зон поражения — `UClanhallHitboxComponent::CheckAndHandleParry` (Раздел 7).                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Активки в клэше не участвуют**                     | WASD парируют WASD, активки контрят активки (`ability_system.md §2`). Выражено данными, а не спецкейсом: у каждой зоны (`FClanhallHitboxDesc`) есть флаг `bParryable`, и `CheckAndHandleParry` вызывается только для зон с `true`. WASD-удары парируемы по дефолту; активки Q/E/R/F ставят `false`, и протухшее `CurrentDirection` последнего WASD-удара до проверки парирования не доходит вовсе.                                                                                                                                                                                              |
| **Отдельных щедрых сфер под парирование НЕ делаем.** | Геометрия оружия защищающегося в проверке не участвует и никогда не участвовала: условие — «зона атаки нападающего коснулась актора, на котором `State.Parrying`, и направление совпало». Это модель Sekiro (тайминг + состояние, а не столкновение оружия об оружие), и она уже самая снисходительная из возможных — парируется то, что в тебя и так попало бы. Щедрые сферы дали бы обратный эффект — парировались бы удары, проходившие мимо, и это читается как случайное срабатывание. Если парировать окажется тяжело — крутить длину окна `State.Parrying`, не размеры зон.              |
| **`UGA_EnemyWASDSeries`**                            | (`Abilities/GA_EnemyWASDSeries.h/.cpp`) — AI-серия ударов, `InstancedPerExecution`, снимает требование стойки, заблокирована `State.Stunned` и `State.ComboRecovery`. Шагает по `AttackDirections` через `AbilityTask_WaitDelay`. На каждый удар (`PrepareHit`): сброс парирования, `State.Parrying` на СЕБЯ (враг — паррируемый актор) на `WindowDuration`, `Parry.Incoming.*` на игрока на то же окно, ожидание. По истечении окна (`OnWindowExpired`): парировано → счётчик; иначе → `ResolveStandardDamage` по игроку (AI получает свои 50 % AP). Между ударами — пауза `DelayBetweenHits`. |
| *Интерим-нюанс*                                      | `State.Parrying` тут вешается кодом — это временная замена `AnimNotifyState_ParryWindow`. Снимается в Разделе 8, когда враг начнёт проигрывать общие удар-монтажи с этим нотифаем, иначе окно откроется дважды.                                                                                                                                                                                                                                                                                                                                                                                 |
| **Правило «всё или ничего»**                         | `FinalizeSeries`: полное парирование (`ParriedCount == число ударов`) → AI оглушён (`State.Stunned` на `StunDuration`) + КД игрока сокращаются на `CDReduction`. Пропустил хоть один — эффекта нет. После серии — `State.ComboRecovery` против мгновенного перезапуска (длительность: при полном парировании `Stun+0.5`, иначе 1.0 сек).                                                                                                                                                                                                                                                        |
| **Сокращение КД игрока**                             | `ReducePlayerCooldowns`: запрос активных эффектов по родительскому тегу `Cooldown` (`MakeQuery_MatchAnyOwningTags` — в UE 5.3+ смотрит и asset-, и granted-теги; наш `GE_ApplyTimedTag` кладёт тег в `DynamicGrantedTags`), считает `остаток − CDReduction`, снимает все КД-эффекты и перевешивает ещё не истёкшие с укороченной длительностью.                                                                                                                                                                                                                                                 |
| **Конкретная серия**                                 | `UGA_Series_Crosscut`:  «Перекрёстный» Часового A→D (ответ игрока D→A). Гранится Training Dummy по таймеру 4 сек.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

---

**Проверка:** болванчик делает A→D, игрок жмёт D→A — парирует и оглушает; пропустил один — эффекта нет.


---

### Раздел 6 — Контрнавык
**Статус:** ✅ Готово.

**Итог:** активка врага, начатая в открытом окне, прерывается любым навыком игрока из её набора `CounteredBy` в момент, когда зона этого навыка коснётся врага, — активка врага уходит на полный КД и получает стан, а навык игрока при этом наносит полный урон и коммитится как обычное применение (Charges/КД — по цене неотличим от рядового удара).

---

| Реализация                                        | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`UClanhallCounterComponent`**                   | (`AbilitySystem/ClanhallCounterComponent.h/.cpp`) — на обоих бойцах, симметрично. `OpenWindow(CounteredBy, CounteredHandle, CooldownTag, CooldownDuration, StunDuration)` запоминает **набор тегов, которыми контрится** текущая активка, её хендл, КД и длительность стана (свойство СБИТОГО навыка — `UAbilityData::CounterStunDuration`), вешает `State.CounterWindow` (loose-тег) на владельца. `IsCounterableBy(Tag)` = окно открыто И `CounteredByTags.HasTag(Tag)` — именно `HasTag`, а не `HasTagExact`: запись `Ability.Skill.Lancer` матчит любой навык Ланцера, так что крупность настраивается в данных без кода. Пустой контейнер = не контрится ничем. `ConsumeCounter` → `CancelAbilityHandle` активки врага + полный КД на неё + `State.Stunned` на `StunDuration` (если > 0, `ApplyTimedTag`; тег уже в `ActivationBlockedTags` у `GA_EnemyWASDSeries` — механический стаггер бесплатно) + `OnCounterConsumed` (`BlueprintAssignable`, точка подключения флинча/VFX/звука — сама реакция не реализована) + закрытие окна. Статический `TryResolveCounter(Target, Tag)` — общий резолвер, который дёргают навыки игрока. |
| **Асимметрия данных**                             | Атакующий предъявляет ОДИН тег — свою идентичность (`UAbilityData::CounterTag` у игрока, `AbilityTags` у врага). Защищающийся держит НАБОР (`CounteredBy` — заведено и в заголовке `UAbilityData`, хотя враг его пока не читает). Набор именно на защищающемся, потому что окно открывает он сам и передаёт контейнер напрямую; держи список на атакующем — каждое открытие окна требовало бы скана всех ассетов.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Контр резолвится ПО КОНТАКТУ, не на активации** | `TryResolveCounter(Target, Data->CounterTag)` вызывается в `GA_PhysicalSkill::ResolveHitOn`, сразу после подтверждённого попадания и до блока метки — тот же паттерн, что у парирования (`UClanhallHitboxComponent::CheckAndHandleParry`): зона атакующего касается тела защищающегося, состояние защищающегося проверяется в момент касания. Свип идёт по тем же зонам, что резолвят урон (`ECC_Pawn`), а не хитбокс-против-хитбокса. Бесплатного контра больше не существует по построению: цена (Charges/КД) — на нажатии, все эффекты контра — на контакте; при успехе навык игрока дополнительно наносит **полный урон** навыка. Промах и недостаточная дальность = контра не было, но заряды и КД всё равно списаны. На фолбэк-пути (`CastMontage == nullptr`) `ResolveHitOn` зовётся прямо на активации — контр остаётся проверяемым на болванчике до нарезки анимаций.                                                                                                                                                                                                                                                                                 |
| **Сторона врага — заменено в Разделе 8 (Блок C)** | Интерим `GA_EnemyActiveSkill.h/.cpp`, описанный здесь ранее (свой `WaitDelay(CounterWindowDuration)`, `OnHitDelayExpired`, дублирующее поле `CounterStunDuration`), снесён целиком. Враг активирует `UGA_PhysicalSkill` с тем же `SourceObject` (`UAbilityData`), что игрок — окно контра открывает `AnimNotifyState_CounterWindow` по общему ассету, а не код способности; удар резолвится контактом хитбокса, а не таймером.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| *Интерим-нюанс (закрыт в Разделе 8, Блок C)*       | окно врага раньше открывалось кодом — временная замена `AnimNotifyState_CounterWindow`. Убрано вместе с переводом врага на `UAbilityData`: нотифай ищет спек по общему ассету так же, как у игрока.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Конкретная активка — теперь общий ассет**        | Бывший `UGA_Enemy_PowerStrike` снесён (Раздел 8, Блок C). Его числа (`Cooldown.Slot.E`, `CounterStunDuration 0.5`, урон) и тег `Ability.Skill.Knight.PowerStrike` живут в общем `UAbilityData` Power Strike (Раздел 4, Раздел 7 Блок D) — отдельного вражеского класса или ассета для них больше нет.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

---

```
**Пройденные этапы:**

- **От «контрит тот же навык» к набору контрящих.** 
  Изначально активку прерывал тот же самый навык — совпадение одного `CounterTag`. Правило заменено на «разные классы контрят разные навыки», чтобы мотивировать носить второе оружие и изучать чужие классы.
  
- **Следствие для данных.**
  Одно поле `CounterTag` тащило два смысла сразу — «кто я» и «кого я контрю»; работало только потому, что это было одно и то же. Связь стала многие-ко-многим → смыслы разведены: идентичность осталась у атакующего, набор `CounteredBy` лёг на защищающегося.
  
- **Побочные выигрыши.** 
  Проверка через `HasTag` вместо сравнения тегов дала крупность записи бесплатно («любой навык Ланцера» или конкретный удар), а пустой контейнер стал естественным способом сказать «не контрится ничем» (War Shout). Самоконтр перестал быть автоматическим и стал явной записью в наборе.
  
- **Цена правила.** 
  Самообучающее свойство старого правила (увидел навык босса → выучи его → контри) утеряно: теперь без плашки босса и боевого журнала правило нечитаемо, и они из украшения стали обязательной частью механики (`ability_system.md §2`, §4).
  
- **От резолва-на-активации к резолву-по-контакту**
  Проверка «в момент нажатия» была тестовым интеримом: механика должна решаться боем, как и парирование. Заодно ушла асимметрия «контр ничего не стоит, обычный удар стоит всё» — теперь контр это тот же удар, просто со сбитым кастом и станом в придачу.
```

---

**Проверка:** 
После этапа 8.

---
### Раздел 7 — Animation Setup
**Статус:** ✅ Готово.

**Итог:** WASD-удары стали направленным деревом комбо: `UClanhallComboComponent` сам валидирует ввод по данным и активирует шаг, `GA_DirectionalAttackBase` больше не решает ничего, кроме урона/MP/Balance. Анимационный слой навешивается поверх готовой механики.

---

| Реализация                                                            | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **`UComboData`**                                                      | (`Fragments/ComboData.h`) — один Data Asset на класс. Модель пар переходов, не путей (канон — `combo_system.md §3`): профиль урона на НАПРАВЛЕНИЕ (`Overhead`/`RightSlash`/`LeftSlash`/`LowSweep`), `StanceAnim`, и пять наборов переходов — `FromStance`, `FromOverhead`, `FromLeftSlash`, `FromRightSlash`, `FromLowSweep`, `Recovery` — 20 слотов итого (4 опенера + 12 переходов + 4 Recovery-хвоста).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Инвариант резолва**                                                 | ход определяется ТОЛЬКО парой «предыдущее направление → новое» (`FindOpenerMontage`/`FindTransitionMontage`); история серии до предыдущего шага не участвует — компонент хранит только `LastDirection`. Повтор направления (`W→W`) непредставим на уровне типа: у `From*`-наборов нет слота на своё же направление. Это не ограничение реализации, а выраженный в типах дизайн: второй такой же удар — не продолжение, а новый удар. Движение то же самое, между ними Recovery — резать клип на такой «переход» не из чего и незачем. Правило сквозное — см. «Правила разработки».                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Ворота ввода, не буфер**                                            | До окна чтения нажатия отбрасываются и не копятся; в окне «последнее решает». Нет накопления → нет залипания отзывчивости.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Ключевой инвариант**                                                | На нажатии решаются **выбор шага, валидация по дереву, потолок ранга и стоимость** (Charges, КД активки) — это не может зависеть от анимации.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Применение урона, MP и Balance у WASD-ударов отдано окну контакта** | `AnimNotifyState_Hitbox` удар засчитывается тогда, когда зона реально касается цели. Механика по-прежнему работает без единой анимации: если у шага нет монтажа или на монтаже не расставлены зоны, `GA_DirectionalAttackBase` резолвит урон мгновенно сферой `TraceRange`/`TraceRadius`. `Montage` хода может быть `nullptr` — дерево комбо проверяется до нарезки анимаций.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Потолок длины серии**                                               | **`AClanhallHumanoidCombatant::ClassRank + 1`** (`ClassRank` 0–4, плейсхолдер до прокачки)  — на персонаже, не в ассете: длина зависит от бойца, а не от оружия. Дерево может содержать записи длиннее ранга — они просто недоступны.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Recovery-анимация ≠ тег `State.ComboRecovery`**                     | Анимация берётся из завершившейся цепочки (`RecoveryMontage`, nullptr = хвост запечён в удар) и играет всегда после терминального удара, кроме выхода из стойки посреди удара. Тег — геймплейный лок-аут НОВЫХ атак И входа в стойку (не выхода), вешается на терминале ТОЛЬКО если Recovery-монтаж реально стартовал: длительность ровно<br>  `RecoveryMontage->GetPlayLength()`, без долей и добавок. Тюнинговых полей (`RecoveryLockFraction`, `ComboRecoveryDuration`) больше нет. Единая точка завершения — `EndSequenceWithRecovery`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Диспетчер зон поражения**                                           | `UClanhallHitboxComponent`Заменил жёстко зашитый трейс оружия. Собственной геометрии не имеет: кость, смещение, поворот, форма (сфера/капсула/бокс), размеры и роль каждой зоны приходят из `FClanhallHitboxDesc`, заданного на `AnimNotifyState_Hitbox` конкретного монтажа. Одновременно<br>  может быть открыто несколько зон, у каждой свой набор задетых целей (`AlreadyHit`) — поэтому «каждый враг на пути рывка — ровно один раз» получается из данных, без отдельного сбора целей по траектории. Пара Begin/End связывается по указателю на нотифай, а не по хендлу: `UAnimNotifyState` — разделяемый const-объект, состояние конкретного проигрывания на нём жить не может.                                                                                                                                                                                                                                                                                                                                                            |
| **Геометрия зоны считается в одном месте**                            | `FClanhallHitboxDesc::GetWorldTransform` / `GetEffectiveHalfHeight` / `MakeCollisionShape` (`ClanhallHitboxTypes.h/.cpp`). Это не удобство, а инвариант: ту же функцию зовёт и свип в рантайме, и превью в редакторе, поэтому нарисованное и бьющее не могут разойтись.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Две точки отрисовки**                                               | В рантайме — `bDrawDebugHitboxes` на компоненте (форма + заметённый путь + точки попадания). В окне монтажа — `UAnimNotifyState_Hitbox::DrawInEditor` (`WITH_EDITOR`), тем же цветом (cyan — `bParryable`, orange — нет). Без второго зоны размечались вслепую: `DrawDebug*` требует `UWorld`, а в Persona нет ни мира, ни актора с компонентом. Граница превью: рисуется зона в позе кадра, а не заметённый свипом объём — на быстром взмахе фактическая область поражения шире нарисованной.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Инвариант «один резолвер на актора»**                               | `Event.Hitbox.Hit`/`Event.Hitbox.Closed` летят на весь ASC без адресации, и система корректна ровно потому, что в любой момент урон по контакту резолвит не более одной способности. Держится на порядке вызовов: `ActivateStep` закрывает зоны предыдущего шага ДО активации следующей способности, а активка гасит серию ДО того, как подпишется на события. `Event.Hitbox.Closed` шлётся СТРОГО на переходе «были зоны → зон не осталось»: вызов `End*` по пустому списку оборвал бы способность, которая активировалась, но зону ещё не открыла.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Поза стойки — в данных класса**                                     | `UComboData::StanceAnim` (`UAnimSequence`, не монтаж — стойка это поза в State Machine, а не монтаж через слот). ABP тянет её через `AClanhallCharacter::GetStanceAnim` в Sequence Player состояния `CombatStance`. Магическая и антимагическая стойки сюда НЕ попадают — они глобальные, одни на все классы (Раздел 9).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Отмена серии чужим монтажом**                                       | `upperbody` и `fullbody` делят `DefaultGroup`, поэтому активка Q/E/R/F прерывает удар-монтаж и без мер заставила бы серию залипнуть. `CancelSequenceForExternalMontage()` гасит состояние без Recovery-анимации и без `State.ComboRecovery` (чужой монтаж уже занимает слот, и наказания за прерывание нет — тот же принцип, что у невалидного продолжения). Монтаж не останавливает: `Montage_Play` вызывающего сам его перебьёт.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Страховки состояния**                                               | Серия сбрасывается и по концу окна, и по концу удар-монтажа (делегат) — даже если нотифая на монтаже нет. `ResetCombo` гасит `bReadWindowOpen`. Зоны поражения закрываются (`ForceEndHitboxes`) при конце/прерывании своего монтажа и при выходе из стойки — но только свои: делегат конца монтажа сверяет идентичность с `LastPlayedMontage` и отсекает старевшие вызовы (он приходит асинхронно, после блендаута, когда владелец зон уже сменился).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Активка убирает свои зоны сама**                                    | Зоны открывает нотифай, а не способность, и съеденный блендаутом `NotifyEnd` оставил бы зону свипаться вечно и бить после конца способности. `UGA_PhysicalSkill::EndAbility` зовёт `UClanhallHitboxComponent::EndHitboxesFromMontage(PlayedCastMontage)`. Ключ идентичности — сам монтаж: `UAnimMontage::Notifies[i].NotifyStateClass` вопреки имени поля есть указатель на инстанс нотифая, и ровно он приходит в `BeginHitbox` как `Source` (`MontageOwnsNotify`) — сравнение указателей, без нового состояния. Грубый `EndAllHitboxes()` запрещён по двум причинам: `EndAbility` приходит асинхронно (при `UDashFragment` — из `OnDashFinished`, много позже конца монтажа, когда зонами владеет уже следующий шаг комбо), а `GA_DirectionalAttackBase` слушает `Event.Hitbox.Closed` — чужая уборка оборвала бы живой WASD-удар. Ненулевое число закрытых зон пишет варн: залипшая зона — ошибка разметки, а не норма. У WASD-ударов этой дыры нет: жизненным циклом удар-монтажа владеет комбо-компонент и страхуется `ForceEndHitboxes()`. |
| **Гейт на данные**                                                    | нет цепочки-опенера на направление → соответствующий WASD не бьёт вообще.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Класс-нейтральность данных**                                        | (задел под ИИ босса): ключ хода `EClanhallAttackDirection` нейтрален к источнику ввода, player-специфики в `UComboData` нет — этим уже пользуется `AClanhallHumanoidBoss` (Раздел 8, Блок B), пока временным прямым вызовом `HandleAttackInput`. `GA_EnemyWASDSeries` остаётся мёртвым кодом до сноса в Разделе 8, Блок D.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Слоты монтажей разведены по типу действия**                         | `upperbody` — WASD-удары, продолжения и Recovery-хвосты; `fullbody` — **все активки Q/E/R/F без исключения**, а не только R/F. Активка отыгрывается целым телом, поэтому разбиение «лёгкие на верх, тяжёлые целиком» отменено. Типовая ошибка — монтаж остался в `DefaultSlot`: логика работает, анимации не видно. Канон — `locomotion_structure.md §3`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Активку нельзя прервать вручную**                                   | Начатая активка доигрывает монтаж целиком: `State.SkillCommitted` живёт до `EndAbility`, а не до первого `Event.Hitbox.Closed`. Единственный способ её сбить — контратака противника своей активкой (`CancelAbilityHandle` из `ConsumeCounter`). Тот же тег гейтит `DoMove` и `CanJumpInternal` — движение и прыжок тоже ручное прерывание. Выход из боевой стойки этим не затронут: `GA_CombatStance` наследуется от `UGameplayAbility` напрямую и `ActivationBlockedTags` базового класса не имеет.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Свобода уйти привязана к слоту, не к типу действия**                | Занято ровно то, что занял монтаж. `upperbody` (WASD-удары, Recovery-хвосты) — ноги свободны, отпустить ЛКМ и убежать посреди доигрывания законно. `fullbody` (все активки) — монтаж занимает всё тело, свободных ног нет, уходить не из чего — без гейта получалось скольжение по земле под целотельной анимацией. Отдельный `State.Busy` сюда не нужен: `State.SkillCommitted` уже значит ровно это и висит ровно столько. Цена: залипший тег теперь стоит управления персонажем, а не только блокировки атак.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Боевая стойка — состояние в Main States**                           | `CombatStance`, Sequence Player, Loop. Пин `Sequence` промоутнут в `CurrentStanceAnim` и заполняется из `Character->ComboData->StanceAnim` в `NativeInitializeAnimation` — не захардкожен, иначе свап оружия (Раздел 10) позу не поменяет. Переход Idle/Locomotion ↔ CombatStance по `bInStance` = `HasMatchingGameplayTag(State.InStance)`, читается в `Event Blueprint Update Animation` (НЕ Thread Safe — ASC из воркер-треда небезопасен). Duration 0.18 в обе стороны = `StanceExitBlendOutTime` комбо-компонента, иначе на выходе ступенька. Ноги — из этого состояния, руки — из слота поверх; монтаж кончился → слот пуст → руки вернулись в стойку без кода возврата.                                                                                                                                                                                                                                                                                                                                                                   |
| **Перемещение навыка — данные, не свойство клипа**                    | `UDashFragment` (`Distance`/`Duration`) + `UAbilityTask_ApplyRootMotionMoveToForce` в `UGA_PhysicalSkill`. Клип режется in-place, `Enable Root Motion` на нём выключен — иначе анимационный root motion сложится с Root Motion Source и рывок улетит вдвое дальше. Фрагмент, а не поле заголовка: его отсутствие значит «навык не двигает персонажа», чего `Distance = 0` не выражает. Следствие: multi-hit у рывка работает только потому, что капсула реально едет — при in-place анимации без фрагмента зона стоит на месте и задеть может лишь одного.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Способность ждёт позднейший из двух терминаторов**                  | `TryFinishAbility()`: `EndAbility` только при `bPrimaryTerminatorFired && !bDashPending`. Без этого фолбэк-путь убил бы задачу рывка в тот же кадр, а короткий монтаж оборвал бы рывок на середине. Первичный терминатор — конец каст-монтажа всегда, когда монтаж стартовал, независимо от режима резолва: размечены ли зоны — вопрос того, как считается урон, а не того, сколько длится отыгрыш. Обратная сторона сохранена осознанно: внешний `CancelAbility` от контра снимает задачу, и персонаж встаёт посреди разгона — это и есть видимый результат сбитой активки.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Окно контра — разметка без данных**                                 | `UAnimNotifyState_CounterWindow` своих полей не несёт. Ключ поиска — сам монтаж: `NotifyBegin` ищет активный спек, чей `UAbilityData::CastMontage` совпадает с проигрываемым, и берёт `CounteredBy`/КД/`CounterStunDuration` оттуда. Пустой `CounteredBy` — окно не открывается вовсе. Один ассет обслуживает и игрока, и врага, которому этот навык выдан, поэтому дублировать теги на дорожке нечего. **`NotifyBegin` и `NotifyEnd` ищут по одному и тому же критерию** — общий `FindOwningAbilityData`: активный спек, чей `CastMontage` совпал с проигрываемым и чей `CounteredBy` непуст. Хендл между Begin и End не хранится, потому что негде: `UAnimNotifyState` — разделяемый const-объект, поэтому критерий пересчитывается заново. Симметрия несущая, а не косметическая: разный критерий дал бы `NotifyEnd`, закрывающий чужое окно — например открытое интеримом врага, у которого ассета ещё нет.                                                                                                                                  |
| **Служебные теги раздела**                                            | `State.ComboRecovery` (лок-аут новых атак и входа в стойку ровно на время Recovery-анимации, только если она реально стартовала), `Event.DirectionalAttack`(несёт `BaseDamage` от компонента к абилке — не notify, выбор способности не гейтит).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Разметка нотифай-стейтов (общее для всех окон)**                    | Montage Tick Type = **Branching Point** — обычные нотифаи обрабатываются на границе тика и на низком FPS съедают кадры, что заметно на окнах в 0.2–0.5 сек. Конец стейта не ставить впритык к концу монтажа — 1–2 кадра запаса, иначе `NotifyEnd` попадёт в блендаут. У зоны с `bParryable = false` (активки Q/E/R/F) направление удара не читается вовсе — ставить `SetCurrentDirection` для таких монтажей не требуется. У шага без Hitbox-нотифая урон резолвится мгновенно на активации (фолбэк) — разметка `AnimNotifyState_Hitbox` обязательна только когда нужен контактный резолв.                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Правило на каждый новый монтаж**                                    | Любой новый источник `Montage_Play` в `DefaultGroup` (реакции на удар, стан, касты) обязан сначала гасить серию через `CancelSequenceForExternalMontage()` — и делать это ДО подписки на `Event.Hitbox.*`, иначе собственный `Closed` от закрываемых зон оборвёт способность до контакта.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

---

```
**Пройденные этапы:**

- **Зоны перестали размечаться вслепую.**
   Первая версия диспетчера имела только рантайм-отрисовку (`bDrawDebugHitboxes`), а `DrawDebug*` требует `UWorld` — в окне монтажа нет ни мира, ни актора с компонентом, поэтому форма и смещения подбирались числами и проверялись только запуском PIE. Закрыто штатным editor-хуком `UAnimNotifyState::DrawInEditor`. Обязательным условием было вынести математику трансформа из `TickHitbox` в саму `FClanhallHitboxDesc` ДО добавления отрисовки: превью со своей копией вычислений рано или поздно расходится с свипом и начинает врать, а врущее превью хуже отсутствующего.

- **Нестартовавший каст-монтаж больше не вешает способность.**
  Если `Montage_Play` возвращал 0 (несовместимый скелет или невалидный слот), зона не открывалась никогда, `Event.Hitbox.Closed` не приходил ни разу, и игрок терял управление боем до перезапуска PIE. Теперь проверка старта идёт ДО `AddLooseGameplayTag`, неудача уводит в тот же мгновенный фолбэк, что и отсутствие зоны, и пишется `UE_LOG(Warning)` — молчаливый отказ читался как «навык сломался», а не как «монтаж не проигрался». Поле `bEndOnHitboxClosed` удалено.

- **Лок-аут активки растянут на весь монтаж.**
  Сначала `State.SkillCommitted` снимался на первом `Event.Hitbox.Closed` — ради отзывчивости хвост анимации после контакта оставался прерываемым WASD-ударом. Отменено дизайн-решением: активка — цельное действие, её отыгрыш нельзя отменить своим вводом, только чужой контратакой. Закрыло открытый вопрос о границах тега.

- **Слот `Cast` отложен до Раздела 9, а не решается здесь.**
  Слот — это не имя, а правило смешивания: `upperbody` подмешивается поверх низа по `spine_01`, `fullbody` перекрывает всё. Третий приёмник оправдан только если каст должен смешиваться одновременно с чем-то ещё либо по другой маске костей, а это следует из формы каста, которая определяется в Разделе 9. Цена откладывания — одна нода в AnimGraph и запись в Anim Slot Manager, поэтому решать вслепую сейчас нечего.
```

---
##### Блок A — Фундамент
**Статус:** ✅ Готово.

| Редактор                                      | Описание                                                                                                                                                                                                                                        |
| --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Сокет `WeaponSocket` на скелете персонажа** | и у игрока, и у врага. Не на скелете оружия: `UClanhallHitboxComponent` ищет кость/сокет только на `ACharacter::GetMesh()` и отдельный меш оружия не опрашивает вовсе. Дефолт зоны поражения (`FClanhallHitboxDesc::Bone`) ждёт именно это имя. |
| **ABP: `Main States`**                        | (локомоция + прыжок) и два слота — `upperbody` и `fullbody`, склейка `Layered Blend Per Bone` по `spine_01`.                                                                                                                                    |
| **Центровка pelvis по X/Y**                   | относительно root на mocap-клипах — свой `UAnimationModifier` (`AM_CenterPelvisXY`), прогоняется на клипе. По Z общего модификатора нет — только визуальная проверка поклипно.                                                                  |

---
##### Блок B — Монтажи игрока и нотифаи
**Статус:** ✅ Готово.

*Статус анимаций:*

| col/row | Stance |  W  |  A  |  S  |  D  |
| :------ | :----: | :-: | :-: | :-: | :-: |
| Stance  |   ❌    |  ✅  |  ✅  |  ✅  |  ✅  |
| W       |   ✅    |  ❌  |  ✅  |  ✅  |  ✅  |
| A       |   ✅    |  ✅  |  ❌  |  ✅  |  ✅  |
| S       |   ✅    |  ✅  |  ✅  |  ❌  |  ✅  |
| D       |   ✅    |  ✅  |  ✅  |  ✅  |  ❌  |

**Mocap**

https://github.com/user-attachments/assets/1a1860be-d227-4b33-a482-516d13a3bf6f

**Sequencer**

https://github.com/user-attachments/assets/30800fd3-17b4-4992-8a20-a2b77ff6b3f6


---

| Редактор                          | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Четыре монтажа-опенера**        | из стойки (W=Overhead, D=RightSlash, A=LeftSlash, S=LowSweep) — строка `Stance` в таблице ниже. Опенер режется именно из стойки: удар из середины дубля на эту роль не годится.                                                                                                                                                                                                                                                                                                                       |
| **`AnimNotifyState_Hitbox`**      | вокруг фазы контакта — на КАЖДОМ удар-монтаже (опенеры и переходы), не на Recovery. Montage Tick Type = **Branching Point**. На WASD-ударах зона остаётся дефолтной (сфера на `WeaponSocket`, `bParryable = true`) — переразмечать существующие монтажи не нужно.                                                                                                                                                                                                                                     |
| **`AnimNotifyState_ComboWindow`** | на каждом удар-монтаже: 70-95% причём End ПОСЛЕ окна контакта — иначе следующий шаг срежет собственное попадание. На Recovery не ставится: он ввод не читает.                                                                                                                                                                                                                                                                                                                                         |
| **Остальные монтажи матрицы**     | 12 переходов между ударами (по 3 из каждого направления) с теми же двумя нотифай-стейтами из п.5–6, плюс 4 Recovery-хвоста «узел → стойка» без нотифаев. Итого по блоку — 20 монтажей: 4 опенера + 12 переходов + 4 Recovery. Клип адресуется ПАРОЙ «предыдущее направление → новое», а не полным путём: `A→W` один на все ветки, откуда бы в `A` ни пришли — второй вариант того же перехода резать некуда. Нарезка узловыми позами и правило «резать из самого длинного дубля» — `combo_system.md`. |

---
##### Блок C — Data Asset комбо-оружия
**Статус:** ✅ Готово.

| Редактор                        | Описание                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`UComboData`**                | один ассет на класс. Заполнить профиль урона — четыре обязательных поля `Overhead`/`RightSlash`/`LeftSlash`/`LowSweep` (`BaseDamage` + `DamageType`-заглушка, в расчёте пока не читается) — и `StanceAnim` (loop-поза боевой стойки, `UAnimSequence`). Назначить ассет в поле `ComboData` на BP-персонаже, там же выставить `ClassRank` — потолок длины серии живёт на персонаже, не в ассете. |
| **Разложить монтажи по слотам** | ереходов по таблице из п.4: `FromStance` (4 опенера), `FromOverhead`/`FromLeftSlash`/`FromRightSlash`/`FromLowSweep` (по 3) и `Recovery` (4 по последнему направлению серии) — 20 слотов, один к одному с монтажами Блока B.                                                                                                                                                                   |
| *Пустой слот*                   | легальное состояние, а не недоделка: `nullptr` в `From*` = продолжение запрещено → серия уходит в Recovery; `nullptr` в `FromStance` = этим направлением серию не начать; `nullptr` в `Recovery` = хвост запечён в сам удар-монтаж.                                                                                                                                                            |
| *Повтора направления*           | (W→W и т.д.) в типах нет — слота просто не существует, заполнять нечего и ошибиться негде.                                                                                                                                                                                                                                                                                                     |
| *Cписка цепочек нет*            | Ни Data Table ходов, ни списка цепочек нет: клип выбирается парой направлений, история серии до предыдущего шага в резолве не участвует.                                                                                                                                                                                                                                                       |

---
##### Блок D — Активки Q/E/R/F (Knight)
**Статус:** ✅ Готово.


**Animation Montage**

https://github.com/user-attachments/assets/65780ea9-4e29-4790-a1eb-3d9732e2fdaa


| Редактор                              | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Разметка зон на каст-монтажах**     | На каждом поставить `AnimNotifyState_Hitbox` вокруг фазы контакта с `bParryable = false` (активки в клэше не участвуют, Раздел 5). Форма — своя на навык, и она отвечает на вопрос «чем я бью», а не «где враг»: Power Strike — капсула вдоль клинка; Shield Slam — сфера на кости щита, малый радиус (не дотянулся — не попал, и это видно в редакторе); Shield Charge — капсула на `pelvis`; War Shout и подобные — большая сфера на `root`, без привязки к оружию. С этого момента навык сам переключается с мгновенного резолва на контактный — кода менять не нужно. |
| `UDashFragment` **Shield Charge (R)** | C++ (Root Motion Source). **`Duration = 0.433`** — не произвольное число. Клип 1.1 с / 33 кадра = 30 fps, фаза движения — кадры 1–13, то есть 13/30 с. Больше — персонаж продолжает лететь поверх кадров восстановления и скользит по земле. **`Distance` — единственное свободное число** (старт 300 см → 6.9 м/с). С ростом дистанции растёт проскальзывание стоп — это потолок подхода.                                                                                                                                                                                |
| **`CounterTag`**                      | в 4 Knight `UAbilityData` (`Ability.Skill.Knight.ShieldSlam / PowerStrike / ShieldCharge / Retribution`) — заполнено. Это **идентичность** навыка («чем я контрю»), не список целей: набор контрящих живёт на защищающемся навыке (Раздел 6).                                                                                                                                                                                                                                                                                                                             |
| **`AnimNotifyState_CounterWindow`**   | на каст-монтажах **игрока** — отрезок, в который навык можно сбить. Своих полей у нотифая нет: `CounteredBy`, КД и `CounterStunDuration` он берёт из `UAbilityData` того навыка, чей `CastMontage` совпал с проигрываемым монтажом. Ставить рядом с Hitbox-нотифаем, помня ограничение из «Открытых вопросов» Раздела 8 — кадр контакта контрящего навыка обязан попадать внутрь этого окна. Разметка ассета одна на обе стороны: тот же ассет, выданный врагу в Разделе 8, даёт врагу то же окно.                                                                        |

---

**Проверка:** в стойке W→A играет цепочку с разными клипами; невалидное продолжение гасит серию без урона и сдвига шкал; удар на потолке `ClassRank` вешает лок-аут; правка `UComboData` меняет дерево без перекомпиляции.


---

### Раздел 8 — Противники
**Статус:** ⏳ В работе — Этап 0, Блоки A/A2/B/C/D/E готовы, дальше Блоки F–H.

**Итог:** враг и игрок стали одним классом бойца на одних данных: `AClanhallHumanoidBoss` наследует ту же ветку, что и `AClanhallCharacter` (`AClanhallCombatantBase` → `AClanhallHumanoidCombatant`), берёт тот же `UClassKitData`, те же `UComboData`/`UAbilityData` и грантится тем же кодом — отдельной «вражеской» логики активок и WASD-серии в прототипе больше не осталось.

---

#### Враг — класс бойца

Противнику вручную назначаются **класс бойца и ранг класса** (`ClassTag` — через `ClassKit`, `ClassRank` — на инстансе), ровно так же, как игроку; системы уровня нет и не будет до появления прокачки (см. «Принятые решения» п.3). Всё остальное — `UComboData`, `UAbilityData`, монтажи и их разметку — он берёт те же, что игрок. Отдельных «вражеских» ассетов не существует ни для одного навыка — их не отложили, их не будет.

Основание уже заложено и не требует нового кода: `UAbilityData` симметричен по построению (Раздел 4), ключ хода `EClanhallAttackDirection` нейтрален к источнику ввода (Раздел 7, «Класс-нейтральность данных»), а Counter- и Mark-компоненты живут на обоих бойцах одинаково.

Что из этого следует:

- **Разметка общая — значит, и её эффекты общие.** `AnimNotifyState_ParryWindow` на удар-монтаже открывает окно тому, кто монтаж играет, то есть и игроку тоже. Это не побочный ущерб, а симметрия: атаки игрока становятся парируемыми по тем же правилам. Механика для этого уже есть — `CheckAndHandleParry` проверяет состояние того, кого задели, без оглядки на сторону; не хватает только AI, умеющего ответить обратным направлением (Блок D).
- **Прогрессии за пределами `ClassRank` в этом разделе нет.** `f(уровень) → ранг/атрибуты` не строим (см. «Принятые решения» п.3): `ClassTag`/`ClassRank` выставляются вручную на инстансе, как у игрока. Цифры Часового и Стража в блоках ниже — ручные и остаются ручными до появления системы прокачки.
- **Драйвер серии решён.** Враг едет на `UClanhallComboComponent` целиком, как игрок, — валидация по дереву комбо достаётся ему бесплатно. `GA_EnemyWASDSeries` — не решение, а мёртвый код: он ещё жив в исходниках, но держит только имущество (симметрия парирования, исход серии), которое переезжает в `UClanhallComboComponent` в Блоке D, после чего класс удаляется. Прежние нелегальные серии (`W→W` у Часового, `D→D→A` у Стража) заменены на `W→S` и `D→A→D` — обе легальны в дереве комбо.
- **Порядок работ.** Сначала враг получает `UAbilityData` через `SourceObject`, потом начинает проигрывать монтажи, и только потом снимаются интеримы (окно парирования, окно контра). Обратный порядок ломает окно контра молча — см. «Открытые вопросы» п.9 ниже. Для Блока C порядок соблюдён; он остаётся в силе и для Блока E.

---

| Реализация | Описание |
| --- | --- |
| **Двухслойная иерархия бойца** | `AClanhallCombatantBase` (ASC, `UClanhallAttributeSet`, Mark, Hitbox, Counter, тег роли) → `AClanhallHumanoidCombatant` (Combo, Parry, `ClassKit`, `ClassRank`, грант WASD-ударов и активок) → `AClanhallCharacter` / `AClanhallHumanoidBoss`. Граница проведена по составу: метка, зоны поражения и окно контра нужны любому бойцу, включая будущего монстра, а комбо-дерево и парирование — только тем, кто дерётся на данных игрока. |
| **`UClassKitData`** | Класс бойца одним ассетом: `ClassTag`, `ComboData`, `TMap<FGameplayTag, UAbilityData*> Skills` с ключом `Cooldown.Slot.*`. Заменил шесть отдельных полей на персонаже. Фрагментов нет намеренно — состав фиксирован, это прецедент `UComboData`, а не `UAbilityData`. Связь направлена кит → тег; реестра `Ability.Class.* → кит` нет и не будет до Раздела 10. |
| **Слот навыка — свойство гранта, не ассета** | `CooldownTag` и `RequiredClass` из `UAbilityData` удалены. Слот приходит ключом карты `UClassKitData::Skills` и доносится до способности динамическим тегом спека (`FGameplayAbilitySpec::GetDynamicSpecSourceTags`), читается через `UGA_PhysicalSkill::GetCooldownSlotTag` по корню `Cooldown.Slot`. Следствие: один `UAbilityData` может лежать в двух китах на разных слотах — с полем в ассете это было невыразимо. `CounterTag` остался в ассете: это идентичность навыка, а не размещение, и `CounteredBy` других ассетов ссылается на него по имени. |
| **Источник ввода комбо нейтрален к стороне** | `HandleAttackInput` — единственная точка входа и для Enhanced Input, и для AI, второй не заводили. Добавлены `OnComboWindowOpened`/`OnComboWindowClosed` и публичный запрос состояния серии — по ним AI узнаёт момент подать направление. Компонент остаётся воротами, а не буфером: AI, не попавший в окно, теряет серию ровно как игрок. |
| **Что снесено** | `UGA_EnemyActiveSkill`, `UGA_Enemy_PowerStrike`, `AClanhallTrainingDummy` с таймерными циклами. Вместе с ними ушёл интерим окна контра по `CounterWindowDuration` — поле не переехало в ассет, а исчезло: длительность окна стала разметкой, а задержка до удара перестала существовать, потому что удар приходит от контакта. |
| **Диагностика вместо молчания** | Варны на пустое значение слота, на невалидный ключ и на ключ, равный корню `Cooldown.Slot`, в цикле гранта; варн в `GA_PhysicalSkill::ActivateAbility` и в `AnimNotifyState_CounterWindow`, когда у спека нет слот-тега. Причина: все три ошибки — редакторские, и без варна дают симптом «навык не нажимается» без следа в логе. |

---

##### Блок A — Иерархия бойца
**Статус:** ✅ Готово.

| Редактор | Описание |
| --- | --- |
| **BP босса** | Новый BP от `AClanhallHumanoidBoss`, скелетал-меш — тот же Manny, что у игрока. |
| **Имя `WeaponTraceComponent`** | перенесено символ в символ при разбиении на `AClanhallCombatantBase`/`AClanhallHumanoidCombatant` — переименование порвало бы BP-данные игрока (см. «Открытые вопросы» п.7). |

---

##### Блок A2 — Кит
**Статус:** ✅ Готово.

| Редактор                      | Описание                                                                                                                                                                                                                 |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **`DA_ClassKit_Knight`**      | `ClassTag = Ability.Class.Knight`, `ComboData` — существующий ассет игрока, четыре записи `Skills` по слотам Q/E/R/F. Назначен в BP игрока и BP босса.                                                                   |
| **Предупреждение на будущее** | ключ карты `Skills` обязан быть листом (`Cooldown.Slot.Q` и т.д.), не корнем `Cooldown.Slot` — запись с корнем грантится, но `GetActiveSkillHandle` её никогда не найдёт (ключ карты другой); цикл гранта на это варнит. |

---

##### Блок B — Источник ввода
**Статус:** ✅ Готово.

| Редактор | Описание |
| --- | --- |
| **`UComboData` босса** | назначен тот же ассет, что у игрока, через кит. |
| **Прогон серии до BT** | временный вызов `HandleAttackInput` (нода `BlueprintCallable`) — Behavior Tree появится в Блоке G. |

---

##### Блок C — Враг на данных игрока
**Статус:** ✅ Готово.

| Редактор | Описание |
| --- | --- |
| **Ассеты активок** | назначены боссу через кит — те же, что у игрока. |
| **`CounterTag`** | в четырёх ассетах Knight уже заполнен (Раздел 7, Блок D) — Блок C переводит чтение врага на `UAbilityData` через `SourceObject`, отдельного заполнения не требовалось. |

---

##### Блок D — Симметрия парирования и исход серии
**Статус:** ✅ Готово. Полная модель — `combat_system.md §5`, здесь только техническая сводка.

Телеграф направления — `Attack.Direction.*`, владелец вешает его на СЕБЯ на время своего замаха, а не на цель: резолв «мой хитбокс дотянулся до тега на цели» физически недостижим по таймингам — защищающийся реагирует на анимацию и объективно отстаёт на собственный замах, встречный тайминг не собрать. Исход серии — шкала `Stagger`, а не бинарное «все N ударов — иначе ничего»: бинарный исход инвертировал стимулы — отступление атакующего среди серии засчитывалось как «полное парирование», а частичное парирование не давало ничего.

| Реализация | Описание |
| --- | --- |
| **Резолв на контакте атакующего** | Окно парирования (`AnimNotifyState_ParryWindow` → `State.Parrying`) размечено на монтаже ЗАЩИЩАЮЩЕГОСЯ; резолв запускает контакт зоны АТАКУЮЩЕГО — свой хитбокс защищающегося в проверке не участвует вообще. |
| **`Attack.Direction.*`** | Тег, который владелец вешает на СЕБЯ на время своего замаха — кодирует направление своего удара, читается тем, кого он заденет. |
| **Разворот кредита** | `UClanhallParryComponent::TryParry` вызывается на компоненте АТАКУЮЩЕГО (владельца зоны, задевшей цель) — клинок атакующего долетает первым, значит и реагирует на клэш он: атакующий получает `Stagger`+хитстоп, защищавшийся — `Charge`+подавление своей зоны. |
| **`Stagger`/`MaxStagger`** (`UClanhallAttributeSet`) | Усталость от парирования вместо бинарного исхода серии. +1 атакующему за каждый отпарированный шаг, на потолке — сброс в 0 + `State.Stunned` атакующему + снижение КД защищавшемуся. Распадается таймером на `UClanhallParryComponent` после паузы без парирований. Одиночное парирование стан не выдаёт никому. |
| **Подавление зон (`bHitboxesSuppressed`, `UClanhallHitboxComponent`)** | Второй сквозной принцип: любой контакт по актору (клэш или пропущенный удар) глушит ЕГО СОБСТВЕННУЮ зону до конца текущего шага — получивший контакт не наносит одновременно и встречный урон тем же замахом. Взведение шлёт `Event.Hitbox.Closed` явно, даже если зона ещё не успела открыться — `UGA_DirectionalAttackBase` на контактном пути завершается ТОЛЬКО по этому событию (терминатора по концу монтажа у неё нет, в отличие от `UGA_PhysicalSkill`), без явной эмиссии способность не закончилась бы вовсе. На `State.SkillCommitted` подавление не влияет — тег снимается только в `UGA_PhysicalSkill::EndAbility` (Раздел 7). Сброс — на следующем шаге/активации владельца. |
| **Хитстоп** | Кратковременное `Montage_SetPlayRate` у того, ЧЬЯ зона нанесла контакт (не у обоих, не у получившего) — на уроне и на клэше, разными длительностями. Без ветвлений по стороне. |
| **Инициализация атрибутов — на `AClanhallCombatantBase`** | Стартовые значения (`DefaultMaxAP/HP/MP/Charges/Stagger`, `EditDefaultsOnly`) выставляются в `AClanhallCombatantBase::BeginPlay`, а не в `AClanhallCharacter::BeginPlay`, как раньше. Причина: `AClanhallHumanoidBoss` собственного `BeginPlay`-пути не имел, и все его атрибуты оставались нулями — `MaxStagger = 0` клампил `Stagger` в `[0,0]`, и проверка `Stagger >= MaxStagger` была истиной уже на первом клэше: босс станился с одного парирования вместо четырёх, при этом шкала выглядела рабочей. |
| **`State.Stunned` блокирует симметрично** | Добавлен в `ActivationBlockedTags` на `UGA_ClanhallAbilityBase` и в ранний гейт `UClanhallComboComponent::HandleAttackInput`. Раньше гейтила только `GA_EnemyWASDSeries` — то есть тег работал исключительно на враге; со шкалой усталости он стал двусторонним, и блокировка обязана быть тоже. |
| **`UGE_ModifyStagger`** | Новый эффект в `ClanhallGameplayEffects`, по образцу `UGE_ModifyCharges`/`UGE_ModifyBalance`. Числа (потолок усталости, задержка/интервал распада, длительности хитстопа, +1 Charge за отпарированный шаг) не дублируются — `combat_system.md §5`. |

**Снесено:** `ParriedStepsThisSeries`/`LastParrierActor`/`LastParriedStepIndex`/`ResolveParryOutcome`/`NotifyStepParried` на `UClanhallComboComponent` — исход больше не свойство серии. `FullParryStunDuration`/`FullParryCooldownReduction`/`ReduceCooldowns` переехали на `UClanhallParryComponent` вместе с новой выплатой.

---

##### Блок E — Монтажи и разметка на враге
**Статус:** ✅ Готово.

| Реализация | Описание |
| --- | --- |
| **`WarnIfHitboxOutlivesCounterWindow`** (`GA_PhysicalSkill`) | Рантайм-варн один раз на монтаж, если зона поражения открывается позже конца окна контра. Частично закрывает «Открытый вопрос» 2 — полноценная редакторная валидация остаётся в бэклоге. |

| Редактор | Описание |
| --- | --- |
| **`AnimNotifyState_Hitbox`** на удар- и каст-монтажах | Кости адресуются именами, существующими на Manny (`WeaponSocket` по умолчанию, `root` у War Shout, `pelvis` у Shield Charge). Босс играет те же монтажи и ту же разметку — отдельных ассетов не заводилось. |
| **`AnimNotifyState_ParryWindow`** на 16 удар-монтажах (4 опенера + 12 переходов) | Начало на кадре 2, конец за кадр до начала первого `Hitbox`-нотифая, отдельным треком. Не ставится на Recovery (это окно наказания) и не ставится на каст-монтажи активок. **Правило разметки — знак легко перепутать:** начало окна задаёт крайний срок реакции (позже начало → строже требование), конец окна задаёт, насколько рано разрешено нажать (раньше конец → строже требование). |
| **`AnimNotifyState_ComboWindow`** | Конец ровно на предпоследнем кадре монтажа. Не позже — на последнем кадре схлопывается кросс-фейд и щёлкает стойка. Не раньше — иначе игрок не успевает стартовать новый монтаж, пока доигрывает предыдущий, и второй шаг серии противника становится непарируемым. |

---

Принятые решения (входные, не обсуждаются внутри блоков)

1. **Враг едет на `UClanhallComboComponent` целиком.** `GA_EnemyWASDSeries` сносится. Валидация серии по дереву `UComboData` достаётся врагу бесплатно; взамен нужен источник ввода вместо Enhanced Input (Блок B).
2. **Общий предок `AClanhallCombatantBase` вытаскивается** из `AClanhallCharacter`. Иерархия двухслойная — см. Блок A.
3. **Уровня как системы нет.** Класс и ранг класса выставляются вручную в инстансе противника, ровно как у игрока (`ClassTag`, `ClassRank`). Никакого `f(level)` → ранг/атрибуты не строим до системы прокачки. Цифры Часового и Стража из Раздела 8 остаются ручными.
4. **Система реакций получателя (флинч) выносится за пределы Раздела 8** в отдельный раздел. Последствие принимается осознанно — см. «Что остаётся сломанным» ниже.
5. **AI: Behavior Tree выбирает действие, C++ исполняет.** BT отвечает на «что сейчас» (видит цель? дистанция? какое действие), C++ — на «как это отыграть» (фазы, кормление ввода в комбо, паузы). Ни одна боевая фаза не живёт в BT-нодах.
6. **Скелет один — стандартный UE Manny** и у игрока, и у противника. Закрывает «Открытый вопрос» 8 требованием, а не проверкой.
7. **Прототип делает только `Unit.Role.Boss.Humanoid`.** `Unit.Role.Mob` и `Unit.Role.Boss.Monster` в этом разделе не реализуются: у них будут свои навыки и свои монтажи, они не едут на данных игрока. Архитектура обязана это допускать — но не обслуживать сейчас.

Что остаётся сломанным после Раздела 8 (осознанно)

- **Контр нечитаем.** Без реакции получателя успешный контр выглядит как обычное применение навыка. Игрок не выучит, что чем сбивается. Чинится в разделе про реакции.
- **Каст-монтаж доигрывает поверх стана.** `ConsumeCounter` зовёт `CancelAbilityHandle`, но каст запущен сырым `Montage_Play` — GAS его не останавливает. Отдельно не чинить: монтаж реакции перебьёт каст сам, когда система появится.
- **Нет редакторной валидации «кадр контакта ≤ окно контра»** («Открытый вопрос» 3). Навык с поздним контактом не сможет контрить никогда и не выдаст ни одной ошибки. В Разделе 8 — только рантайм-варн (Блок E), полноценная проверка в редакторе в бэклог.

---

### Часовой (Dummy) — Блок G, не начат

**Характеристики:** AP 150 / HP 300 / MP 0 / Charges 2 / DT 8

**Что делаем:**
- Behavior Tree или простой State Machine
- Две WASD-серии: A→D («Перекрёстный»), W→S («Вертикаль»)
- «Вертикаль» появляется при HP < 60%. Это та же пара, что у Стража — намеренно: болванчик учит серии, которую потом покажет босс
- Активный навык: **Power Strike** (идентичный тег Ability.Skill.Warrior/Knight → контрнавык работает)
- Накладывает на игрока BROKEN GUARD при попадании
- При HP < 50%: Power Strike после каждой второй серии, агрессия если BROKEN GUARD на игроке
- Пауза 1.5 сек после промаха Power Strike

**Результат:** первый полноценный боевой цикл с противником. Парирование, контрнавык, метка на себе — всё проверяется.

### Стартовый босс (Старый Страж) — Блок H, не начат

**Характеристики:** AP 300 / HP 700 / MP 80 / Charges 4 / DT 12

**Что делаем:**

WASD-серии:
- «Вертикаль» W→S (Фаза 1, 2, 3)
- «Тройной удар» D→A→D (Фаза 1, 2, 3)
- «Широкий крест» A→D→W (Фаза 2, 3 — появляется при HP < 70%)

Активные навыки:
- **Overhead Slam** (2 Charges) — тег Ability.Skill.Warrior.OverheadSlam → накладывает STAGGERED на игрока. Контрнавык: Warrior R
- **War Shout** (0 Charges) — утилита, накладывает BROKEN GUARD на игрока. Контрнавыком не блокируется

Заклинание (хардкод для этого раздела, без полной системы магии):
- **Fire Spark** (W→S) — телеграф: иконки слогов + аудио 1.0 сек, конус 3м, накладывает BURNING на игрока
- Антимагия: недоступна (игрок не знает слогов)

Опасные цепочки меток на игроке:
- War Shout (BROKEN GUARD) → Overhead Slam → +50% урон + игнор 50% DT
- Overhead Slam (STAGGERED) → War Shout → Knockdown 1.5 сек
- Fire Spark (BURNING) → War Shout → игрок в панике −30% урон 3 сек

Фазы:
- Фаза 1 (100–70%): серии + Overhead Slam раз в 2 серии + Fire Spark каждые 20 сек
- Фаза 2 (70–40%): добавляется «Широкий крест» + War Shout перед сериями + Fire Spark каждые 12 сек
- Фаза 3 (<40%): темп +20%, Fire Spark каждые 8 сек, Overhead Slam без паузы

После победы: кат-сцена → гримуар открывается → слоги Wîn (W) и Sîl (S) записываются → разблокировано Ранг 1 Стихий.

**Результат:** полный тестовый босс. Все системы до магии проверены. Первое знакомство с кастом.


---

#### Открытые вопросы (наследство Раздела 7)

1. **[Открыт — Блок F]** **Гейт движения по `State.SkillCommitted` не работает на врагах.** `DoMove`/`CanJumpInternal` — путь Enhanced Input, то есть только игрок. В Разделе 8 врагам-воинам выдадут те же `UAbilityData` через `GA_PhysicalSkill` — тег на них повиснет, а движение идёт через AIController и `CharacterMovement` мимо `DoMove`, и враг поедет по земле под целотельной анимацией точно так же, как игрок до появления гейта по `State.SkillCommitted`. Гейт придётся ставить на другом уровне (движение или AI-логика), а не повтором той же проверки. Единая точка обязана спрашивать не только про `State.SkillCommitted`, но и про `State.Stunned` — сейчас стан блокирует действия, но не движение, и оглушённый спокойно уходит с места. Раньше это было незаметно (тег вешался только на врага), после симметричной шкалы усталости (Блок D) дыра проявится сразу у обеих сторон.
2. **[Открыт]** **«Кадр контакта ≤ окно контра» — молчаливая невозможность.** После перехода контра на контакт зона обязана дотянуться до врага внутри его окна. Если у навыка кадр контакта позже окна, он **не сможет контрить никогда**, и ни одной ошибки нигде не появится — искать будут в логике контра, а причина в разметке. Нужна проверка в редакторе или хотя бы рантайм-варнинг. Тот же расчёт нужен для `CounterStunDuration`: стан должен переживать дорогу твоего удара до цели.
3. **[Открыт — вынесен в отдельный раздел вне Раздела 8, см. «Принятые решения» п.4]** **Система реакций получателя.** `ImpactMontage` был удалён осознанно: реакция принадлежит получателю. После перехода контра на контакт флинч врага стал **несущим**: контр выглядит точно так же, как обычное применение навыка, и единственный сигнал «контр сработал» — реакция врага. Без неё игрок не выучит, какой навык что сбивает. Открытое: форма системы (GameplayCue или делегат), кто владеет монтажами реакций, и сколько их видов (срыв каста и обычное попадание — разные реакции). Сюда же примыкает смежная дыра: при контратаке `ConsumeCounter` зовёт `CancelAbilityHandle`, но каст-монтаж запущен сырым `Montage_Play`, а не `PlayMontageAndWait` — GAS его не останавливает, и замах доигрывает поверх стана (рывок при этом снимается, так что «встал посреди разгона» работает). Отдельно её чинить не нужно: монтаж реакции перебьёт каст сам, как только система появится.
4. **[Закрыт — Блок E]** **Удары врага не имели зон.** Раньше `OnHitDelayExpired` (`GA_EnemyActiveSkill`, снесён в Блоке C) применял урон по таймеру, без касания — бой был односторонним: твои удары решались геометрией, его — таймером. Диспетчер зон (`UClanhallHitboxComponent`) враг унаследовал через `AClanhallCombatantBase` (Блок A) с самого начала; не хватало только разметки `AnimNotifyState_Hitbox` на его монтажах — она встала в Блоке E. Враг играет те же монтажи, что игрок, с той же разметкой, отдельных ассетов нет.
5. **[Открыт]** **Одна и та же зона от одного нотифая дважды одновременно.** `EndHitbox(Source)` закрывает все зоны с этим Source. Если один и тот же нотифай окажется активен на двух параллельных монтажах одного актора, конец первого закроет обе. Общие монтажи на игрока и врага этого НЕ обостряют: `ActiveHitboxes` живёт на компоненте конкретного актора, и двое бойцов, играющие один ассет, друг другу зоны не гасят. Для прототипа приемлемо; если понадобится точность — ключом станет пара (Source, MeshComp).
6. **[Открыт]** **Имя сабобъекта `WeaponTraceComponent`** оставлено ради сохранности BP-данных и теперь расходится с именем класса. Компонент теперь живёт на `AClanhallCombatantBase` (перенесён Блоком A, имя сохранено символ в символ). Если BP-наследник персонажа этот компонент не переопределяет, имя можно смело переименовать — но проверить это может только разработчик, из кода `.uasset` не читается.
7. **[Открыт]** DO NOT Блока A запрещал поднимать `UClanhallComboComponent`, `UClanhallParryComponent` и `ComboData` в `AClanhallCombatantBase`. Принято решение: **все бойцы, включая существ, бьют по четырём направлениям WASD** — анимации свои, механика общая. Значит компоненты обязаны жить в общем предке, а на `AClanhallHumanoidCombatant` остаётся только `ClassKit`. Исполнение — когда появится первое существо: Часовому и Стражу (гуманоиды) переезд ничего не даёт, а правка задевает грант способностей и BP обеих сторон.
8. **[Открыт]** Ключ карты `UClassKitData::Skills` — `Cooldown.Slot.*`. Имя переживёт кулдауны и станет врать. Переименование в `Ability.Slot.*` — в `task_skill_economy_concept.md`, не здесь.
9. **[Открыт]** **Хитреакт на пропущенном ударе — аддитивная поза, не слот-монтаж.** Слот-монтаж перебил бы удар-монтаж и уронил серию в нейтраль (см. п.3 выше — система реакций). Реализация — отдельная нода в AnimGraph поверх, а не через слот. Как v0 сойдёт вовсе без анимации: тряска камеры + вспышка на материале. Территория `locomotion_structure.md`.
10. **[Открыт]** **Нет сигнала «мой шаг отпарировали».** Нужен Блоку G: без него у босса нет условия прервать серию, когда его читают (отступление под парированием). Естественное место — делегат на `UClanhallParryComponent`, вещается из `TryParry` рядом с инкрементом `Stagger`. Делать одним заходом с двумя делегатами `UClanhallComboComponent` из «Открытых вопросов Раздела 11», п. 4.1 («шаг активирован», «серия завершена») — один и тот же файл, один поход.
11. **[Открыт]** **Хитстоп: восстановление `PlayRate` может потеряться.** `ApplyHitstop` берёт `GetCurrentActiveMontage()` и держит один `FTimerHandle` на компонент. При двух одновременных монтажах в разных слотах замедлится верхний, не обязательно тот, чья зона ударила; второй хитстоп по другому монтажу отменит восстановление первого, и тот останется на `PlayRate 0.15` насовсем. Сейчас параллельных монтажей нет — станут возможны вместе с хитреактом (п.9), чинить не позже него.
12. **[Открыт]** **Рывок и анимация расходятся под хитстопом.** У активок с `UDashFragment` рут-моушен-таск идёт своим таймером, `Montage_SetPlayRate` его не замедляет. Косметика, заметна на Shield Charge.
13. **[Открыт]** **Отображение `Stagger` на HUD** — на плашке босса и у игрока. Постоянной полоской места нет (AP/HP/MP/Charges + метка уже там), вероятно всплывающий индикатор или пипсы. Задача `hud_dev_plan.md`.
14. **[Открыт]** **Инициализация атрибутов через DataAsset/GameplayEffect** вместо хардкода в `AClanhallCombatantBase::BeginPlay` (см. строку «Инициализация атрибутов» в таблице Блока D). Текущие `DefaultMax*` — плейсхолдеры прототипа.
15. **[Открыт]** **Читаемость поз телеграфа.** Игрок выбирает направление ответа только по анимации противника, за окно порядка полусекунды (Блок E, `AnimNotifyState_ParryWindow`). Если на приёмке Блока G направление удара босса не читается по позе — временно вернуть отладочный вывод `INCOMING [X]`, а в план поставить доработку поз. Костыль, не решение.


---
### Раздел 9 — Магическая система

**Что делаем:**
- RMB зажат = режим каста, движение заблокировано
- 8 клавиш как syllable input (Q/A/W/S/E/D/R/F в режиме каста)
- Lookup-таблица: комбо слогов → `GameplayAbility`
- Ранг 1 Стихий (разблокированный Стражем): Fire Spark (W→S), Frost Stomp (S→W)
- Синергии с метками через те же Fragments (MarkTriggerFragment на спеллах)
- Grimoire UI минимум: страница с комбо, описанием, иконкой
- Разблокировка ранга: триггер после победы над боссом → запись слогов в книгу
- Антимагия Режим А: RMB+LMB, те же слоги, прерывает каст босса, мана не тратится, игрок получает +25 MP; у магии игрока КД нет (только MP)
- Антимагия Режим Б: те же слоги на уже активный зональный эффект, тратит MP
- Ранг школы игрока гейтит антимагию

#### Три стойки — расширение боевой стойки Раздела 2

До этого раздела стойка одна (`State.InStance`, ЛКМ). Магия делает её трёхзначной. Все три стойки взаимоисключающие и все три блокируют движение — WASD/8 клавиш заняты действиями.

| Стойка | Вход | Тег | Что делают клавиши |
|---|---|---|---|
| Боевая | ЛКМ | `State.InStance` | направленные удары, комбо-дерево Раздела 7 |
| Магия | ПКМ | `State.Casting` | слоги |
| Антимагия | ЛКМ+ПКМ | `State.CastingAntimagic` | слоги, Режим А/Б |

**Стойка — чистая функция от зажатых кнопок мыши,** а не машина состояний с переходами по событиям. При каждом изменении набора зажатых кнопок стойка пересчитывается заново. Переключение между всеми тремя свободное, возврат в Idle не требуется: игрок начал каст, увидел замах босса, дожал ЛКМ — уже в боевой и парирует.

**Гейт — занятость, а не ввод.** Пересчёт заморожен, пока персонаж отыгрывает действие: висит `State.Busy`, его ставит абилка удара/каста и снимает по завершении. Гейтить по `IsAnyMontagePlaying` нельзя — между концом удар-монтажа и стартом Recovery есть кадры без монтажа, и в ту же slot-группу попадают посторонние монтажи (реакции на удар, эмоции), которые блокировали бы стойку без причины. Каст обязан ставить `State.Busy` независимо от своей механики (мгновенный, канал, прицеливание) — иначе стойка переключится посреди каста.

**Частичное отпускание — не событие, а новое значение аргумента.** Отпустил ЛКМ из антимагии, ПКМ держит → как только `State.Busy` спадёт, окажется в магии. Отдельной обработки не требует.

**Отпущены все кнопки мыши — единственное сквозное событие,** работает поверх `State.Busy`: стойка снимается, низ уходит в локомоцию. Recovery доигрывает в своём слоте (upperbody) поверх локомоции — не обрывается выходом из стойки; стык между Recovery и бегом закрывается блендаутом слота, отдельного клипа под каждую цепочку не нужно. Живой удар-монтаж (в отличие от Recovery) обрывается `Montage_Stop` с блендаутом. Канон — `UClanhallComboComponent::OnStanceExit` (Раздел 7), то же поведение распространяется на все три стойки.

**Анимация:** три состояния (боевая/магия/антимагия) в Main States — там же, где боевая стойка Раздела 7, полный меш переходов между ними; нейтраль = обычная локомоция, отдельного `Relaxed`-состояния нет. Источник ассета асимметричен: боевая тянется из `ComboData.StanceAnim` через переменную (per-class — позиция оружия своя у каждого класса), магия и антимагия захардкожены в своих Sequence Player (глобальные, одни на все классы). Кратковременный проход через боевую при «ЛКМ → ПКМ через 30 мс» гасится инерциализацией 0.15–0.2 с — окна разрешения ввода не нужны. Длительность переходов держать близкой к блендауту слота `upperbody`, иначе на стыке ступенька.

**Новые теги раздела:** `State.Stance` (umbrella — вешают все три стойки-абилки дополнительно к своему; `AClanhallCharacter::DoMove` гейтится по нему вместо перечисления трёх, теги не переименовываем — добавляем), `State.Busy` (лок пересчёта стойки на время отыгрыша действия).

**Результат:** игрок открывает гримуар, кастует Fire Spark, может сделать антимагию на Стража.

---
### Раздел 10 — Смена оружия и финальная интеграция

**Что делаем:**
- Weapon slot система: клавиши 1–6
- Swap AbilitySet при смене: RemoveAbilities старого набора → GrantAbilities с нового DataAsset
- Эффекты перегруза Balance: при выходе за ±60 → GameplayEffect: навыки перегруженной стороны ×2 Charges
- Добавить второе оружие (Warrior / Assassin) для теста свопа
- Сквозной тест всего цикла: два оружия, два противника, метки в обе стороны, магия

**Результат:** рабочий прототип. Смена оружия меняет набор навыков, Balance-штраф работает, весь боевой цикл проверен.

---
### Раздел 11 — Искусственный интеллект
**Статус:** не начат; стартует после Раздела 10.

**Итог:** боевой прототип даёт противнику ровно столько поведения, сколько нужно для проверки боевых систем; Раздел 11 заменяет эту заглушку архитектурой AAIRV из `ai_dev_plan.md` — сценарные фазы становятся следствием целей, а не скриптом.

Канон раздела — `ai_dev_plan.md`, здесь только граница и стык. При расхождении между документами главнее боевые доки — это правило уже записано в шапке самого `ai_dev_plan.md`.

---

#### Граница: что остаётся Разделу 8, что забирает Раздел 11

| Раздел 8 (прототип) | Раздел 11 заменяет на |
|---|---|
| Behavior Tree выбирает действие, C++ исполнитель отыгрывает фазы | UtilityEvaluator + цели с гистерезисом (`ai_dev_plan.md §3`), GOAP-планировщик A*, MaxDepth = 3 (`§6`) |
| Фазы Стража по порогам HP — скриптом | сдвиг BaseValue целей по HP-порогам; цепочки меток строит планировщик сам (`§6`) |
| Опасные цепочки меток заданы списком | эмерджентны из `UGoapFragment` (`§2`) |
| Минимальный счётчик повторов серии под парирование (Блок G) | полноценная `FPlayerModel` + Озарения с телеграфом, этап AI-4 (`§4`, `§5`) |
| Противник платит Charges/КД как игрок | то же правило, без изменений — ИИ не получает читов исполнения (`§7`) |

Последняя строка важна: это единственное, что переходит из прототипа в ИИ без переделки.

#### Этапы

Порядок и названия — из `ai_dev_plan.md §11`, туда же за подробностями и критериями проверки каждого этапа. Раздел не начат целиком, статусов этапам здесь не проставляем:

- **AI-1** — `FCombatWorldState` из GAS + `UGoapFragment` + ручной план (без A*) на Часовом
- **AI-2** — `GOAPPlanner` (A*, MaxDepth=3) + цель BuildSynergy
- **AI-3** — `UtilityEvaluator` + гистерезис + цели ProbePlayer/AvoidDamage/DenyPlayer
- **AI-4** — `FPlayerModel` + Озарения «Прочитал серию/навык» + телеграф
- **AI-5** — Немезис: сериализация `FPlayerModel`, затухание
- **AI-6** — L3: событийная очередь, заказы одного клана, псевдобой единой формулой
- **AI-7** — Дирижёр: ходы кланов, генерация заказов, летопись → слухи
- **AI-8** — Рельсы: принудительные utility для сюжетных сцен

#### Открытые вопросы Раздела 11

##### 4.1 У WASD-серии нет ключа повтора (приоритетный)

Дыра между двумя параграфами `ai_dev_plan.md`: у действия «Парирование серии игрока» (`§2`) предусловие — «`PlayerModel`: повтор серии ≥ порога», а `FPlayerModel.ActionCounters` (`§4`) ключуется `GameplayTag`. У WASD-серии тега нет и быть не может — серия это упорядоченный набор направлений, а не способность, то есть предусловие `§2` не опирается ни на одну структуру `§4`.

**Решение — ключом становится префикс, а не серия целиком.** Хранится не «серия → вес», а «префикс → распределение следующего направления». Обоснование:

- Отпарировать N-й шаг противник обязан выбором обратного направления, пока ещё идёт (N−1)-й — предсказание работает над префиксом, счётчик на завершённую серию физически не успевает.
- «Забывание» получается само: свернул с накатанного префикса — предсказание не сбылось, шаг проходит; отдельной логики сброса `streak` не нужно.
- Награда за вариативность пропорциональна тому, как рано игрок свернул с накатанного, а не бинарна, как у счётчика на целую серию.
- Чередование (`ABAB`) закрывается тем же механизмом: общий префикс копит вес, даже если ни одна серия целиком не повторяется дважды.
- Полное парирование (а значит и стан Блока D) наступает ровно тогда, когда предсказан каждый шаг — правило стана не приходится подпирать отдельным условием.

**Форма:** ключ префикса — упакованные направления (2 бита на шаг, максимум 5 шагов по `ClassRank + 1`), значение — счётчики по четырём продолжениям. На завершении серии: наблюдённой паре `+1`, прочим продолжениям того же префикса `−1` до нуля.

**Где живёт:** компонент на противнике (зародыш `FPlayerModel`), по образцу Mark/Counter — НЕ `UClanhallComboComponent`: тот хранит только `LastDirection`, историю копит наблюдатель (несущий инвариант Раздела 7 — история внутри компонента сломает резолв ходов). Наблюдение пошаговое, а не по факту завершённой серии — модель обязана быть готова к предсказанию уже к третьему шагу: нужны ещё два делегата на `UClanhallComboComponent`, рядом с добавленными в Блоке B — «шаг активирован (направление)» и «серия завершена».

**Порог** — `UPROPERTY` на компоненте с переопределением в экземпляре, не в `UClassKitData`: это сложность конкретного противника, а не содержание класса. У Часового и Стража он обязан отличаться.

**Телеграф обязателен.** `ai_dev_plan.md §5`: незаметная адаптация = несуществующая. Молча начавший парировать босс даёт игроку двухсекундный стан без объяснения — это наказание вместо читаемости. В момент взятия порога нужен видимый тель («он тебя прочитал»).

**Разделение объёма:** минимальный срез (префиксный счётчик + порог + телеграф) — Блок G Раздела 8, ровно столько, чтобы противник парировал заслуженно. Полная система — этап AI-4, где компонент поглощается `FPlayerModel`.

##### 4.2 Перенесено из бэклога

Пункт 1 «Бэклога пост-прототипа» («Боссы контрят спам», см. `ability_system.md §5`; гейт: совпадение сабкласса + порог 3 повтора подряд, плюс симметричное `CounteredBy` у навыков игрока) был помечен «после Раздела 10» — по смыслу принадлежит сюда, раздела тогда просто не существовало.

Отличие от `4.1`: там ключ — `GameplayTag` навыка, и `FPlayerModel §4` его выражает без проблем. Проблема ключа касается только WASD-серий.

---

### Раздел 12 — Колесо классов и перков

![progression_wheel](https://github.com/user-attachments/assets/e188554d-f339-4724-95f7-a9533642664e)

---
## Бэклог пост-прототипа

Следующие механики **не входят в Разделы 1–10**, реализуются после боевого прототипа:

1. **Боссы контрят спам** — перенесено в Раздел 11, «Открытые вопросы Раздела 11», п. 4.2 (было помечено «после Раздела 10» ещё до появления самого раздела).
2. **AP-рефилл** (замена боевого хила). Прототип: клавиша V восстанавливает 50% макс. AP,  ограниченные заряды. Полная экономика (возврат зарядов за босса/волны, зеркальный босс-рефилл   по бездействию, отображение на плашках) — дизайн в combat_system §1, доводится после боевого   прототипа. HP лечится только в городе. N зарядов и объём возврата — TBD.Базовую версию (V = +50% AP, счётчик зарядов) можно ставить в прототип рано — простой GameplayAbility + расход атрибута-счётчика. Экономику возврата зарядов оставить на потом.
3. **Магические печати-пины** — замки на дверях/сундуках, открываемые набором слогов (известная последовательность = ключ). Слоги как ключи к исследованию мира.
4. **Аннигиляция кастов** — редкое событие: оба участника выпустили одинаковые слоги в пересекающемся окне → заклинания аннигилируют, взрыв в центре, отброс обоих.
5. **Debug-читы** (можно раньше, полезно для тестов): консольные команды `Clanhall.GrantCharges N`, `Clanhall.ApplyMark Mark.X`, `Clanhall.SetBalance N`, `Clanhall.SetAP N`.
6. **Editor-валидатор DataAssets:** у FMarkSynergy заполнено ровно одно из EffectOnTarget/EffectOnSelf; каждая потребляемая метка кем-то накладывается; теги существуют.
7. **Асимметрия возврата AP** (игрок 50% / босс 30–40%) — проверить на плейтесте.


---
# План Разработки HUD — Рабочий трекер
## Контекст

- **Движок:** UE 5.8, плагин GAS.
- **Персонаж:** `AClanhallCharacter` (C++ класс, Blueprint-наследник в Content). ASC висит **прямо на персонаже** (не на PlayerState).
- **Атрибуты:** `UClanhallAttributeSet` на ASC. Ноды `Get Gameplay Attribute Value (ASC, Attribute, bFound)`.
- **TargetingComponent:** на Character, `BlueprintReadOnly`. Получать: `GetOwningPlayerPawn → Cast ClanhallCharacter → TargetingComponent`. Это **мягкая цель под удар/метку** — EnemyFrame его больше не слушает
- **BossSensorComponent:** на Character, `BlueprintReadOnly`. Получать: `GetOwningPlayerPawn → Cast ClanhallCharacter → BossSensorComponent`. Это **драйвер Enemy Frame**: держит `Unit.Role.Boss.*` юнитов в радиусе (`EnterRadius`/`ExitRadius` гистерезис), вещает `OnFrameUnitEntered(AActor*)` / `OnFrameUnitExited(AActor*)`. N боссов = N рамок.
- **OnTargetChanged:** объявлен `BlueprintAssignable` на TargetingComponent, для мягкой цели — не для EnemyFrame.
- **SaveHUDLayout / LoadHUDLayout:** API подтверждён, вызывать `SaveHUDLayout` в `On Mouse Button Up` после перетаскивания. `LoadHUDLayout` возвращает bool (false = первый запуск → дефолты).
- **Дефолтные позиции рамок:** Player Frame `(50, 700)`, Enemy Frame `(1870, 700)`.
- **State.InStance:** тег существует в `ClanhallGameplayTags` (для crosshair).
- **Balance:** диапазон −100..+100, центр 0, данные из `GetBalance()`. Рисуем двумя встречными барами.
- **EnemyFrame — важная правка:** `GetAbilitySystemComponent` **не висит напрямую на `AActor`** в Blueprint. Правильная нода: `Ability System Blueprint Library → Get Ability System Component (Actor)`, передать `NewTarget`. TargetingComponent уже фильтрует цели по `IAbilitySystemInterface` перед записью в `CurrentTarget`, поэтому ASC вернётся валидный — но null-check оставить (враг мог умереть между тиками).

---
## Статус виджетов

| #   | Виджет                                      | Статус      |
| --- | ------------------------------------------- | ----------- |
| 1   | `WBP_AttributeBar`                          | ✅ Готово    |
| 2   | `WBP_Diamond`                               | ✅ Готово    |
| 3   | `WBP_ChargesPanel`                          | ✅ Готово    |
| 4   | `WBP_Balance`                               | ✅ Готово    |
| 5   | `WBP_PlayerFrame`                           | ✅ Готово    |
| 6   | `WBP_EnemyFrame` + `WBP_BossFrameContainer` | ✅ Готово    |
| 7   | `WBP_Crosshair`                             | ✅ Готово    |
| 8   | `WBP_HUD`                                   | ✅ Готово    |
| 9   | Drag + Save                                 | ✅ Готово    |
| 10  | Alt-режим                                   | ✅ Готово    |
| 11  | Тултипы                                     | 🟡 Отложено |

---
## 1. WBP_AttributeBar 
**Статус:** ✅ Готово.

Универсальный (один класс на HP / AP / MP). ASC приходит снаружи от родителя.
**Переменные (все Instance Editable где нужно):**
- `TargetASC` — `AbilitySystemComponent` (Object Ref), Instance Editable
- `TrackedAttribute` — `Gameplay Attribute`
- `MaxAttribute` — `Gameplay Attribute`
- `FillColor` — `Linear Color`

**Иерархия:** `Overlay` → `ProgressBar` "Bar" (Fill/Fill) + `TextBlock` "ValueText" (center). Оба — `Is Variable`. Дефолтный Canvas удалён.

**Percent биндинг (`GetBarPercent`):** `IsValid(TargetASC)?` → False: return `0.0`. True: `Cur = GetGameplayAttributeValue(TrackedAttribute)`, `Max = GetGameplayAttributeValue(MaxAttribute)`, `Max  0?` → False: `0.0`, True: `Cur/Max` → `Clamp 0..1` → return. (Clamp страхует от перелива AP max.)

**Text биндинг (`GetBarText`):** `Cur`/`Max` → `Round` → `Format Text "{0}/{1}"`.

**Event Construct:** `Bar → Set Fill Color and Opacity (FillColor)`.

**Апгрейд потом:** заменить property binding на `WaitAttributeChanged → SetPercent/SetText`.

---
## 2. WBP_Diamond
**Статус:** ✅ Готово.

Один ромб. Три визуальных состояния (нет / пуст / залит).

**Иерархия:**
- Корень `Size Box` — **обязательно задать `Width Override` / `Height Override`** (напр. 24×24). Без override схлопывается в 0 → не видно.
- Внутрь `Overlay`.
- Два `Image` в Overlay (порядок: нижний в дереве = виден сверху на экране):
  - `Img_Outline` — контур ◇, снизу, виден **всегда** (= «ромб есть, но пуст»).
  - `Img_Fill` — заливка ◆, сверху, `Is Variable`.
- Обоим: Alignment слота Fill/Fill. Если текстуры квадратные — `Render Transform → Rotation = 45` на каждом.

**`SetFilled(bFilled)`:** `Set Visibility (Img_Fill)` → `bFilled ? Visible : Hidden`. Контур не трогаем.

**Event Construct:** `SetFilled(false)` — дефолт пустой.

---
## 3. WBP_ChargesPanel
**Статус:** ✅ Готово.

**Что решено (финальный дизайн):**
- Ромбы = экземпляры `WBP_Diamond`, генерятся программно.
- Текстура/цвет/заливка — целиком внутри `WBP_Diamond` (панель про них не знает). Переменных `DiamondTexture`/`FilledColor`/`EmptyColor` на панели **нет**.
- Засечки `2 | 4 | 6` — **НЕ паддингом**. Статичная текстура-линейка под рядом ромбов: `Overlay` → снизу картинка-линейка, сверху `Row`. Ноль кода. (Позиции засечек фиксированы, т.к. ромбы фиксированного размера, лишние просто `Collapsed`.) Программный путь (считать засечки) — опционально позже, если размеры ромбов станут настраиваемыми в рантайме.

**Переменные:**
- `TargetASC` — `AbilitySystemComponent` (Object Ref), Instance Editable
- `Diamonds` — массив `WBP_Diamond` (Object Ref)

**Иерархия:** дефолтный Canvas удалён. `Overlay` → (низ) `Image` линейка-засечки → (верх) `Horizontal Box` "Row" (`Is Variable`).

**`BuildDiamonds`:** `Clear Children(Row)` → `Clear(Diamonds)` → **For Loop 0..11** → в теле: `Create Widget (WBP_Diamond, Owning Player = Get Owning Player)` → `Add Child to Horizontal Box (Row)` → `Add (Diamonds, ромб)`. Ни Set Brush, ни SetFilled тут нет.

**`Refresh`:** `Cur = GetGameplayAttributeValue(Charges) → Round`, `Max = GetGameplayAttributeValue(MaxCharges) → Round`. `For Each (Diamonds)` с индексом: `Index < Max?` → `Set Visibility` (Visible / Collapsed); `Index < Cur?` → `SetFilled` (true / false).

**`InitWithASC(NewASC)` — Custom Event, НЕ Function.** В Blueprint Function нельзя async/latent-ноды, а подписка `WaitAttributeChanged` именно такая — поэтому только Custom Event в Event Graph. Снаружи вызывается по ссылке как обычная функция. Цепочка:
`Set TargetASC(NewASC)` → `BuildDiamonds` → **`WaitDelay` (один кадр) → `On Finish` → `Refresh`** → подписки `WaitAttributeChanged` на `Charges` и `MaxCharges` (каждая `On Attribute Changed → Refresh`).

⚠ **`WaitDelay` после `BuildDiamonds` обязателен.** Свежесозданные `WBP_Diamond` (через `Create Widget`) не проходят построение/prepass в том же кадре — немедленный `Refresh` отрабатывает по ещё не готовым ромбам, и значения `Charges` не отображаются. Один кадр задержки (`WaitDelay` с ~0 длительностью, срабатывает `On Finish` на следующий тик) откладывает `Refresh` до готовности ромбов.

**Event Construct:** пустой. Вся инициализация (build + refresh + подписки) переехала в Custom Event `InitWithASC`, который вызывает родитель. Временный прокид ASC в Construct (использовался при отладке) — удалён, иначе панель инициализируется на невалидном ASC раньше времени.

---
### 5. WBP_Balance (−100..+100, центр 0)
**Статус:** ✅ Готово.

Обычный ProgressBar не годится — нужен центр-якорь. `Horizontal Box` → два `ProgressBar` по 50% ширины:
- левый: `Bar Fill Type = Right To Left`, `Percent = (Balance < 0) ? -Balance/100 : 0`, синий (DEX)
- правый: `Bar Fill Type = Left To Right`, `Percent = (Balance  0) ? Balance/100 : 0`, красный (STR)
Стык = ноль. Сверху вертикальная риска в центре + отметки на ±60 (граница перегруза, `combat_system.md`).

**Расположение:** шкала вынесена из `PlayerFrame` в `WBP_HUD` с якорем низ-центр экрана (глобальное состояние боя логично держать по центру, как индикатор стойки, а не в углу с ресурсами).

**Отклонённый вариант (на будущее):** «бегунок по статичной шкале» — статичный фон-градиент DEX→STR + маленький `Image`-указатель, ездящий по X от `Balance`. Рабочий, но для боя хуже: крупную заливку видно периферийным зрением, за тонкой рисочкой в замесе надо охотиться глазами. Оставлен как косметический апгрейд, механики не касается.

---
### 5. WBP_PlayerFrame
**Статус:** ✅ Готово.

ASC: `Get Owning Player Pawn → Cast ClanhallCharacter → Get Ability System Component`. `Event Construct`: `IsValid(PlayerASC)?` → True → `Set Target ASC` на HP/AP/MP барах (они на поллинге, сеттера достаточно) → `InitWithASC(PlayerASC)` на `ChargesPanel` (Custom Event). Цвета/`TrackedAttribute`/`MaxAttribute` баров задаются в Details каждого инстанса, НЕ из родителя.
- AP → `AP`/`MaxAP`, жёлто-золотой
- HP → `HP`/`MaxHP`, оранжево-красный
- MP → `MP`/`MaxMP`, сине-фиолетовый
**Компоновка:** HP / AP / MP — три отдельные полосы в `Vertical Box`. Нахлёст AP на верхние 20% HP убран — рядом с MP-баром смотрелся неравномерно; для прототипа стек чище и читаемее. Под барами — `ChargesPanel`. **Balance здесь НЕТ** — переехал в `WBP_HUD` (низ-центр экрана). Рамка всегда видима (никакой логики visibility, в отличие от `EnemyFrame`).

---
### 6. WBP_EnemyFrame + WBP_BossFrameContainer
**Статус:** ✅ Готово.

Рамку порождает/удаляет `WBP_BossFrameContainer`, по одной на каждого босса в радиусе (драйвер — `UClanhallBossSensorComponent`, двойной радиус EnterRadius/ExitRadius; НЕ рейкаст).
N боссов = N рамок.

WBP_EnemyFrame — «тупая», без сенсора и без логики видимости:
- Event Construct пустой.
- `SetupForUnit(NewASC: AbilitySystemComponent)` — `Custom Event: Set Target ASC` на HP/AP/MP → `` InitWithASC(NewASC)` на ChargesPanel →` HideNotUsedBars(NewASC)`.
- `HideNotUsedBars(NewASC)` — функция, Sequence 1–3: `GetGameplayAttributeValue(MaxAP/MaxMP/MaxCharges)<=0 ? Collapsed : Visible` на AP-бар / MP-бар / ChargesPanel. HP всегда виден.
  Так одна рамка показывает `Boss.Humanoid` (HP+AP+MP+Charges), `Boss.Monster` (HP±Charges), Часового (MP скрыт).
- `WBP_ChargesPanel.Refresh`: в начале гард `IsValid(TargetASC)` (при churn рамок ASC часто невалиден).
  Подписки `WaitAttributeChanged` гасятся при удалении рамки (иначе утечка).

WBP_BossFrameContainer — Vertical Box (FramesBox) + Map<Actor, WBP_EnemyFrame (Frames):
- Event Construct: BossSensorComponent → Bind OnFrameUnitEntered/OnFrameUnitExited.
- OnFrameUnitEntered(Unit): Get ASC (Ability System BP Library, Actor=Unit) → null-check →
  Create Widget(WBP_EnemyFrame) → SetupForUnit(ASC) → Add Child(FramesBox) → Frames.Add(Unit).
- OnFrameUnitExited(Unit): Frames.Find(Unit) → погасить подписки charges → Remove from Parent → Frames.Remove(Unit).
- Вставка контейнера в боевой WBP_HUD — в Разделе 8 (пока в тестовом HUD).
- Строка с именем босса над рамкой, плашка активной метки, иконки навыков + КД (combat_system.md) — отдельным проходом позже.

---
### 7. WBP_Crosshair
**Статус:** ✅ Готово.

- Иерархия: `Size Box` (Width/Height Override, напр. 12×12, иначе схлопывается) → `Image` "Dot" (браш-точка; временно однотонный, если текстуры нет). Дефолтный Canvas удалён. Центрирование делает родитель (`WBP_HUD`, якорь center, alignment 0.5/0.5) — внутри самого виджета не центрируем.
- Переменная: `TargetASC` (AbilitySystemComponent, Object Ref, Instance Editable).
- **ASC приходит от родителя `WBP_HUD` через Function `SetCrosshairASC(NewASC)`** — самозахват в `Event Construct` НЕ используется (убран). Функция обычная (не Custom Event): виджет на поллинге, async-подписок нет.
- Visibility-биндинг (`GetCrosshairVisibility`): `IsValid(TargetASC)?` → False: `Collapsed`; True: `Has Matching Gameplay Tag(State.InStance) ? Visible : Collapsed`. Гард `IsValid` обязателен (ASC может быть ещё не проставлен в первый кадр).
- Без анимации, исчезает мгновенно.

---
### 8. WBP_HUD + сборка 
**Статус:** ✅ Готово.

- Корень `Canvas Panel`. Дети (4):
  - `WBP_PlayerFrame` — якорь top-left, дефолт `(50, 700)`.
  - **`WBP_BossFrameContainer`** — якорь top-left, дефолт `(1870, 700)`. ВАЖНО: `EnemyPos` из `LoadHUDLayout` применяется к слоту **контейнера**, а не к отдельной `WBP_EnemyFrame` (рамки боссов лежат стеком в `Vertical Box` контейнера).
  - `WBP_Crosshair` — якорь center `(0.5,0.5)`, alignment `(0.5,0.5)`, позиция `(0,0)`.
  - `WBP_BalanceBar` — якорь низ-центр `(0.5,1.0)`, alignment `(0.5,1.0)`, X `0`, Y отрицательный (напр. `-80`).
- **Раздача ASC централизована в `WBP_HUD` (единая точка правды).** `Event Construct`: `Get Owning Player Pawn → Cast ClanhallCharacter → Get Ability System Component → IsValid?` → True → `SetBalanceASC(ASC)` + `SetCrosshairASC(ASC)`. `PlayerFrame` и `BossFrameContainer` свой ASC достают сами (игрока / врагов через сенсор) — их HUD не трогает.
- В `Event Construct` также наполняется массив `DraggableFrames` (см. раздел 9): `Add(PlayerFrame)`, `Add(BossFrameContainer)`.
- Загрузка раскладки: `LoadHUDLayout(→PlayerPos, →EnemyPos):bool` → Branch. False → дефолты из дизайнера (ничего не делаем). True → `PlayerFrame → Slot → Cast Canvas Panel Slot → Set Position(PlayerPos)`; `BossFrameContainer → Slot → Cast Canvas Panel Slot → Set Position(EnemyPos)`.
- Спавн: в `BeginPlay` персонажа — `Is Locally Controlled?` → `Create Widget(WBP_HUD, Owning Player = Get Controller → Cast PlayerController)` → (опц.) промоут в переменную `HUDWidget` на будущее → `Add to Viewport`. `Owning Player` обязателен, иначе `Get Owning Player Pawn` внутри детей вернёт null.

---
### 9. Перетаскивание + сохранение (централизовано в `WBP_HUD`)
**Статус:** ✅ Готово.

Изначальный план предполагал оверрайды мыши в каждой рамке — сделано иначе. Вся drag-логика живёт в `WBP_HUD` (там же, где ASC и сейв). Перетаскиваемые рамки — `WBP_PlayerFrame` и `WBP_BossFrameContainer` — **оверрайдов мыши НЕ имеют** («тупые»). Требование к ним одно: `Visibility = Visible` (иначе не хит-тестятся). Базовый класс `WBP_DraggableFrame` не создавался — не нужен.

Переменные в `WBP_HUD`:
- `DraggableFrames` — массив `Widget` (наполняется в Construct: PlayerFrame, BossFrameContainer). Порядок = приоритет захвата при наложении рамок.
- `ActiveFrame` — `Widget` (что тащим сейчас; класс не важен, нужен только слот).
- `DragOffset` — `Vector2D` (точка захвата внутри рамки, чтобы не прыгала углом).

`On Mouse Button Down` (оверрайд на `WBP_HUD`):
- `MouseAbs = Mouse Event → Get Screen Space Position`.
- `Set ActiveFrame = null` (сброс перед поиском).
- `For Each Loop (DraggableFrames)` → `Element → Get Cached Geometry → Is Under Location(MouseAbs)` → если True: `Set ActiveFrame = Element` → `Break`.
- `Branch(IsValid(ActiveFrame))`: True → `CanvasGeo = Canvas → Get Cached Geometry`; `DragOffset = Absolute To Local(CanvasGeo, MouseAbs) − (ActiveFrame → Slot → Cast Canvas Panel Slot → Get Position)`; вернуть `Handled → Capture Mouse(Self)`. False → `Unhandled` (клик по пустоте проваливается дальше, ничего не тащим).

`On Mouse Move`:
- Гард `Has Mouse Capture(Self)? AND IsValid(ActiveFrame)?` → иначе `Unhandled`.
- `MouseLocal = Absolute To Local(CanvasGeo, Screen Space Position)`.
- `NewPos = MouseLocal − DragOffset`.
- `ActiveFrame → Slot → Cast Canvas Panel Slot → Set Position(NewPos)` → `Handled`.
- `Absolute To Local` сам учитывает DPI — ручной коррекции скорости нет.

`On Mouse Button Up`:
- Exec-провод: `Branch(IsValid(ActiveFrame))` → True → `SaveCurrentLayout` → `Set ActiveFrame = null`.
- Event Reply (в Return Node): `Handled → Release Mouse Capture → Return Value`. (Reply-ноды цепляются друг за друга через пин `Reply`; `SaveCurrentLayout` вешается по белому exec-проводу, не в reply-цепь.)

`SaveCurrentLayout` (Function на `WBP_HUD`): читает позиции ОБЕИХ рамок поимённо и пишет обе разом (`ActiveFrame` тут не участвует — сохраняем всю раскладку целиком):
- `PlayerFrame → Slot → Cast Canvas Panel Slot → Get Position` → `PlayerPos`.
- `BossFrameContainer → Slot → Cast Canvas Panel Slot → Get Position` → `EnemyPos`.
- `SaveHUDLayout(PlayerPos, EnemyPos)`.

⚠ **Ограничение на будущее:** `SaveHUDLayout` (C++) жёстко на две позиции. Движение масштабируется через массив `DraggableFrames` свободно, но третья перетаскиваемая рамка не сохранится, пока сигнатуру `SaveHUDLayout` не расширят (массив/структура позиций). Для текущих двух рамок всё сходится.

Заметка: `Is Under Location` на `BossFrameContainer` даёт True только когда контейнер непустой (есть босс в радиусе → есть площадь `Vertical Box`). Пустой контейнер не хватается — таскать нечего, это корректно.

---
### 10. Alt-режим 
**Курсор по Alt — ✅ готово.** 

Логика на `PlayerController` (методы `Set Input Mode` / `Show Mouse Cursor` — его). `IA_HUDCursor` (Digital bool) с двумя маппингами `Left Alt` + `Right Alt` в существующем IMC (новый контекст не добавляли). Через события Enhanced Input `Started` / `Completed` (one-shot, БЕЗ опроса в Tick и без ручного bool-флага):
- `Started` (Alt зажали): `Show Mouse Cursor = true`; `Set Input Mode Game and UI` с параметрами: **In Widget to Focus = пусто** (иначе крадёт клавиатурный фокус, глохнет WASD), **Mouse Lock Mode = Do Not Lock**, **Hide Cursor During Capture = false** (иначе курсор мигает/пропадает при клике).
- `Completed` (Alt отпустили): `Set Input Mode Game Only`; `Show Mouse Cursor = false`.

---
### 11. Тултипы
🟡 отложено (заметка на будущее):** 

`On Mouse Enter` на иконках атрибутов/навыков → `Show Tooltip Widget`. Фундамент готов — у рамок уже `Visibility = Visible`, хит-тест под курсором работает.

---

## License
Copyright © 2026 Lilu Dev

This source code is provided for portfolio and review purposes only.
You may view the code on GitHub.
You may not copy, modify, redistribute, or use this code in any project without explicit written permission from the author.